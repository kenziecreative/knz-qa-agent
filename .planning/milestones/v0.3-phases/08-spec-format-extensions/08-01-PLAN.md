---
phase: 08-spec-format-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/SPEC-FORMAT.md
  - agents/qa-tester.md
  - skills/run/SKILL.md
  - examples/visual-testing-spec.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "A spec with ## Visual Focus triggers Tier 2 visual verification awareness in the agent"
    - "A spec with ## Design Reference provides mockup image paths the agent can reference per viewport"
    - "A spec with ## Browsers lists engines the skill loops over, and the agent labels findings per engine"
    - "SPEC-FORMAT.md documents all three new sections with behavior, examples, and available values"
    - "Existing specs without new sections continue to work unchanged (backward compat clause updated)"
  artifacts:
    - path: "examples/SPEC-FORMAT.md"
      provides: "Documentation for Visual Focus, Design Reference, and Browsers sections"
      contains: "### Visual Focus (v2)"
    - path: "agents/qa-tester.md"
      provides: "Visual Focus Trigger, Design Reference handling, Browsers sections"
      contains: "### Visual Focus Trigger"
    - path: "skills/run/SKILL.md"
      provides: "Agent invocation template forwarding blocks for all three new sections"
      contains: "Visual Focus"
    - path: "examples/visual-testing-spec.md"
      provides: "Sample spec demonstrating all three new sections together"
      contains: "## Visual Focus"
  key_links:
    - from: "examples/SPEC-FORMAT.md"
      to: "agents/qa-tester.md"
      via: "Documented spec sections match agent trigger sections"
      pattern: "Visual Focus.*Design Reference.*Browsers"
    - from: "skills/run/SKILL.md"
      to: "agents/qa-tester.md"
      via: "Skill forwarding blocks pass spec content to agent"
      pattern: "Visual Focus Trigger"
    - from: "examples/SPEC-FORMAT.md"
      to: "Backward Compatibility clause"
      via: "New sections listed as optional in compat clause"
      pattern: "Visual Focus.*Design Reference.*Browsers"
---

<objective>
Add three new optional spec format sections (Visual Focus, Design Reference, Browsers) to the QA plugin's spec format, agent instructions, and skill invocation template. These sections provide the infrastructure for all downstream v0.3 visual/UX methodology (Phases 9-12) to be spec-driven.

Purpose: Phase 8 installs the vocabulary and triggers. The actual Tier 2 visual methodology comes in Phases 9-12. This phase makes it possible for specs to declare what kind of visual testing they want, provide design reference images, and specify which browser engines to test.

Output: Updated SPEC-FORMAT.md, qa-tester.md, skills/run/SKILL.md, and a new visual-testing-spec.md example file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-spec-format-extensions/08-CONTEXT.md
@.planning/phases/08-spec-format-extensions/08-RESEARCH.md
@.planning/phases/08-spec-format-extensions/08-PATTERNS.md

<interfaces>
<!-- The three source files and their key insertion points. Executor MUST read these files in full before editing. -->

From examples/SPEC-FORMAT.md:
- Line 183-212: "### Accessibility Focus (v2)" -- the EXACT pattern to mirror for "### Visual Focus (v2)"
- Line 139-181: "### Environments (v2)" -- the structural pattern for "### Design Reference (v2)"
- Line 126-131: "### Viewports" -- the structural pattern for "### Browsers (v2)"
- Line 214: "### Test Scenarios" -- the section AFTER which the new sections are inserted (new sections go BEFORE this)
- Line 385: Backward Compatibility clause -- the line to extend with new section names

From agents/qa-tester.md:
- Lines 719-731: "### Accessibility Focus Trigger" -- the EXACT pattern to mirror for "### Visual Focus Trigger"
- Line 732: End of Accessibility Focus Trigger block
- Line 733: "## Memory & Persistence" -- the section AFTER which new sections are inserted (new sections go BEFORE this, after line 731)

From skills/run/SKILL.md:
- Lines 99-191: Agent Invocation template -- the area receiving new conditional forwarding blocks
- Lines 108-138: Existing conditional blocks ({If spec-based:}, {If environment profile active:}, etc.) -- the pattern for new blocks
- Line 135: {If ad-hoc:} -- new blocks go BEFORE this line
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three new Section Reference entries to SPEC-FORMAT.md</name>
  <files>examples/SPEC-FORMAT.md</files>
  <read_first>
    - examples/SPEC-FORMAT.md (full file -- understand current section ordering and patterns)
    - .planning/phases/08-spec-format-extensions/08-PATTERNS.md (Pattern 1-4 for exact structural templates)
    - .planning/phases/08-spec-format-extensions/08-CONTEXT.md (D-01 through D-14 locked decisions)
  </read_first>
  <action>
Add three new Section Reference entries to `examples/SPEC-FORMAT.md`. Insert them immediately AFTER the `### Accessibility Focus (v2)` block (which ends at line 212) and BEFORE `### Test Scenarios` (currently line 214). The ordering is: Visual Focus, then Design Reference, then Browsers.

Also add corresponding sections to the Basic Structure example at the top of the file (lines 7-90), placed after `## Accessibility Focus` and before `## Test Scenarios`.

**1. Add `### Visual Focus (v2)` section (per D-01, D-02, D-03):**

Mirror the Accessibility Focus (v2) block exactly (lines 183-212). The new section:

```markdown
### Visual Focus (v2)

Controls the depth of visual verification testing. When present, the agent runs Tier 2 visual checks (the full visual methodology). When absent, only baseline Tier 1 visual observations run (anomalies noted during functional testing).

**Behavior:**

- Absent: Tier 1 only (visual anomalies noted during functional testing). This is the default.
- Present with no items listed: Full Tier 2 (all visual areas)
- Present with specific areas listed: Tier 2 for those areas only

**Available focus areas:**

- `design-verification` — compare live page against design reference images (Phase 9)
- `ux-states` — verify empty, loading, error, and interaction states (Phase 10)
- `layout-integrity` — spacing, alignment, grid integrity, content clipping (Phase 11)
- `performance-responsive` — Core Web Vitals, cross-browser rendering, viewport sweep (Phase 12)

**Example (full Tier 2):**

```markdown
## Visual Focus
```

**Example (specific areas):**

```markdown
## Visual Focus
- design-verification
- layout-integrity
```
```

**2. Add `### Design Reference (v2)` section (per D-04, D-05, D-06, D-07):**

Mirror the Environments (v2) structural pattern (keyed subsections). The new section:

```markdown
### Design Reference (v2)

Provides mockup image paths for visual comparison during design verification testing. Uses keyed subsections per viewport with state-specific image paths as nested bullet items.

**Subsection format:**

- Each viewport gets a `### viewport-name` subsection (e.g., `### desktop`, `### mobile`)
- Each subsection contains bullet items in the format: `- state-name: .qa/designs/filename.png`
- Paths are relative to the project root

**Behavior:**

- Section absent: No design comparison runs (backward compatible)
- Section present, viewport subsection present: Agent uses listed images as reference for that viewport
- Section present, viewport subsection absent: Agent skips visual diff for that viewport and notes "no design reference for [viewport]" in the report

**Example:**

```markdown
## Design Reference

### desktop
- home-logged-out: .qa/designs/desktop-home-logged-out.png
- home-logged-in: .qa/designs/desktop-home-logged-in.png
- checkout-step-1: .qa/designs/desktop-checkout-1.png

### mobile
- home-logged-out: .qa/designs/mobile-home-logged-out.png
```
```

**3. Add `### Browsers (v2)` section (per D-08, D-09, D-10, D-11):**

Mirror the Viewports section pattern (simple bullet list). The new section:

```markdown
### Browsers (v2)

Specifies which browser engines to test. When present, the qa-run skill runs the full test suite once per listed engine. When absent, Chromium is used (current default behavior).

**Valid values:**

- `chromium` — Chromium-based rendering (Chrome, Edge)
- `webkit` — WebKit-based rendering (Safari)
- `firefox` — Firefox/Gecko rendering

**Behavior:**

- Section absent: Chromium only (backward compatible)
- Section present: Run each listed engine via `playwright-cli --browser=<engine>`

**Example:**

```markdown
## Browsers
- chromium
- webkit
```
```

**4. Update the Basic Structure example (lines 7-90):**

Add these three sections to the example spec between `## Accessibility Focus` and `## Test Scenarios`:

```markdown
## Visual Focus (optional)
- design-verification
- layout-integrity

## Design Reference (optional)
### desktop
- home-default: .qa/designs/desktop-home.png

## Browsers (optional)
- chromium
- webkit
```

**5. Update the Backward Compatibility clause (currently line 385, per D-12):**

Change the existing text from:
```
All v1 specs continue to work without modification. The v2 fields (`tags:`, `depends_on:`, `## Data Sets`, `## Environments`, `## Accessibility Focus`) are all optional. A spec with none of these fields behaves exactly as before.
```

To:
```
All v1 specs continue to work without modification. The v2 fields (`tags:`, `depends_on:`, `## Data Sets`, `## Environments`, `## Accessibility Focus`, `## Visual Focus`, `## Design Reference`, `## Browsers`) are all optional. A spec with none of these fields behaves exactly as before.
```

**CRITICAL per D-13:** Do NOT create a combined "v3 features" example block. Each section has its own independent example within its Section Reference entry.

**Do NOT edit the existing `### Accessibility Focus (v2)` block** -- it stays exactly as-is.
  </action>
  <verify>
    <automated>grep -n "### Visual Focus (v2)" examples/SPEC-FORMAT.md && grep -n "### Design Reference (v2)" examples/SPEC-FORMAT.md && grep -n "### Browsers (v2)" examples/SPEC-FORMAT.md && grep "Visual Focus.*Design Reference.*Browsers" examples/SPEC-FORMAT.md</automated>
  </verify>
  <acceptance_criteria>
    - examples/SPEC-FORMAT.md contains "### Visual Focus (v2)" heading
    - examples/SPEC-FORMAT.md contains "### Design Reference (v2)" heading
    - examples/SPEC-FORMAT.md contains "### Browsers (v2)" heading
    - examples/SPEC-FORMAT.md contains "design-verification" in Visual Focus section
    - examples/SPEC-FORMAT.md contains "ux-states" in Visual Focus section
    - examples/SPEC-FORMAT.md contains "layout-integrity" in Visual Focus section
    - examples/SPEC-FORMAT.md contains "performance-responsive" in Visual Focus section
    - examples/SPEC-FORMAT.md contains ".qa/designs/" in Design Reference example
    - examples/SPEC-FORMAT.md contains "chromium" and "webkit" and "firefox" in Browsers section
    - Backward compat clause contains "## Visual Focus", "## Design Reference", "## Browsers"
    - "### Accessibility Focus (v2)" block is unchanged (lines 183-212 content preserved)
    - "### Visual Focus (v2)" appears AFTER "### Accessibility Focus (v2)" and BEFORE "### Test Scenarios"
    - Basic Structure example (top of file) includes Visual Focus, Design Reference, and Browsers as optional sections
  </acceptance_criteria>
  <done>SPEC-FORMAT.md has three new Section Reference entries (Visual Focus, Design Reference, Browsers) with correct behavior docs, examples, and valid values. Backward Compatibility clause lists all three new sections as optional. Basic Structure example includes all three. No existing sections modified.</done>
</task>

<task type="auto">
  <name>Task 2: Add Visual Focus Trigger, Design Reference, and Browsers sections to qa-tester.md</name>
  <files>agents/qa-tester.md</files>
  <read_first>
    - agents/qa-tester.md (full file -- understand placement context around lines 719-733)
    - .planning/phases/08-spec-format-extensions/08-PATTERNS.md (Core pattern to mirror from lines 719-731)
    - .planning/phases/08-spec-format-extensions/08-RESEARCH.md (Code Examples section with exact template text)
    - .planning/phases/08-spec-format-extensions/08-CONTEXT.md (D-01 through D-14)
  </read_first>
  <action>
Add three new sections to `agents/qa-tester.md`. Insert them immediately AFTER line 731 (end of `### Accessibility Focus Trigger`, which ends with "...The Accessibility Focus section only controls whether Tier 2 also runs (and which parts of it).") and BEFORE `## Memory & Persistence` (currently line 733).

**Do NOT edit lines 719-731 (Accessibility Focus Trigger) -- they stay exactly as-is.**

**1. Add `### Visual Focus Trigger` section (per D-01, D-02, D-03, D-14):**

```markdown
### Visual Focus Trigger

The `## Visual Focus` section in a spec controls visual verification depth:

- **Section ABSENT:** Run only baseline Tier 1 visual observations (visual anomalies noted during functional testing — things like broken images, obvious layout breaks, or missing elements). This is the default.
- **Section PRESENT with no items listed:** Run FULL Tier 2 visual verification — all areas (design-verification, ux-states, layout-integrity, performance-responsive).
- **Section PRESENT with specific areas listed:** Run Tier 2 visual checks for only the listed areas. Valid area names:
  - `design-verification` — compare live page against design reference images (Phase 9)
  - `ux-states` — verify empty, loading, error, and interaction states (Phase 10)
  - `layout-integrity` — spacing, alignment, grid integrity, content clipping (Phase 11)
  - `performance-responsive` — Core Web Vitals, cross-browser rendering, viewport sweep (Phase 12)

**This is a depth toggle, not an on/off switch.** Baseline Tier 1 visual observations always run regardless of this section's presence. The Visual Focus section only controls whether Tier 2 also runs (and which parts of it).

**Note:** Tier 2 visual methodology for each area will be added in Phases 9-12. If a Visual Focus section is present in a spec before those phases ship, note in the report: "Visual Focus requested for [areas] — full methodology pending Phase [N]."
```

**2. Add `### Design Reference` section (per D-04, D-05, D-06, D-07, D-14):**

```markdown
### Design Reference

When a `## Design Reference` section is present in the spec, use the provided image paths during visual verification:

- The section uses keyed subsections per viewport (`### desktop`, `### mobile`, etc.)
- Each subsection contains bullet items in the format: `- state-name: .qa/designs/filename.png`
- Paths are relative to the project root

**Behavior:**

- If a viewport subsection is present: use its images as reference during visual comparison for that viewport
- If a viewport subsection is absent: skip visual diff for that viewport, note "no design reference for [viewport]" in the report
- If the `## Design Reference` section is absent entirely: no design comparison runs (backward compatible)

The actual comparison methodology (how to compare live screenshots against reference images) is implemented in Phase 9.
```

**3. Add `### Browsers` section (per D-08, D-09, D-10, D-11, D-14):**

```markdown
### Browsers

When a `## Browsers` section is present, the qa-run skill runs the test suite once per listed engine. The active engine is passed to you in the test assignment header.

**Your responsibility:** Include the active engine name in your report header and findings. Example: "[webkit] Login form — PASS". If you notice rendering differences between engines (reported across multiple runs), note them as cross-browser findings.

**Valid engines:** `chromium`, `webkit`, `firefox`

**Default (section absent):** Chromium only — same as current behavior.
```

All three sections go in sequence after the Accessibility Focus Trigger, with a blank line between each. The `## Memory & Persistence` heading follows after the last new section.
  </action>
  <verify>
    <automated>grep -n "### Visual Focus Trigger" agents/qa-tester.md && grep -n "### Design Reference" agents/qa-tester.md && grep -n "### Browsers" agents/qa-tester.md && grep -n "### Accessibility Focus Trigger" agents/qa-tester.md</automated>
  </verify>
  <acceptance_criteria>
    - agents/qa-tester.md contains "### Visual Focus Trigger" as a heading
    - agents/qa-tester.md contains "### Design Reference" as a heading (within the Spec Format v2 Features area, not elsewhere)
    - agents/qa-tester.md contains "### Browsers" as a heading (within Spec Format v2 area)
    - Visual Focus Trigger contains all four area names: "design-verification", "ux-states", "layout-integrity", "performance-responsive"
    - Visual Focus Trigger contains "This is a depth toggle, not an on/off switch."
    - Visual Focus Trigger contains "full methodology pending Phase"
    - Design Reference contains "no design reference for [viewport]"
    - Design Reference contains ".qa/designs/"
    - Browsers contains "[webkit]" example label
    - Browsers contains "chromium", "webkit", "firefox" as valid engines
    - "### Accessibility Focus Trigger" block is unchanged (still present, content intact)
    - "## Memory & Persistence" still appears after the new sections
  </acceptance_criteria>
  <done>qa-tester.md has three new agent instruction sections (Visual Focus Trigger, Design Reference, Browsers) inserted between Accessibility Focus Trigger and Memory & Persistence. All four visual focus areas documented. Placeholder wording for pending methodology included. Existing Accessibility Focus Trigger untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add forwarding blocks to skills/run/SKILL.md and create sample spec</name>
  <files>skills/run/SKILL.md, examples/visual-testing-spec.md</files>
  <read_first>
    - skills/run/SKILL.md (full file -- understand agent invocation template structure)
    - examples/sample-login-spec.md (pattern for the new example spec file)
    - .planning/phases/08-spec-format-extensions/08-PATTERNS.md (Skill pattern section, shared patterns)
    - .planning/phases/08-spec-format-extensions/08-RESEARCH.md (Code Examples for skill + agent blocks)
    - .planning/phases/08-spec-format-extensions/08-CONTEXT.md (D-08 through D-14)
  </read_first>
  <action>
**Part A: Update skills/run/SKILL.md**

Add three new conditional forwarding blocks to the Agent Invocation template. Insert them AFTER the existing `{If spec has data-driven scenarios:}` block (which ends around line 134) and BEFORE `{If ad-hoc:}` (currently line 135).

Add the following blocks:

```markdown
{If spec has Visual Focus section:}
## Visual Focus
**Areas**: {area list, or "all (full Tier 2)" if present but empty}
Run Tier 2 visual checks per your Visual Focus Trigger.

{If spec has Design Reference section:}
## Design Reference
{full content of the ## Design Reference section from the spec, including viewport subsections and image paths}

{If spec has Browsers section:}
## Browser Engines
**Engines to test**: {comma-separated engine list from spec}
**Active engine for this run**: {current engine}
Run the full test once per engine. Report results labeled by engine: "[chromium] Scenario X: PASS"

{If spec has no Browsers section:}
## Browser Engines
**Engine**: chromium (default)
```

Also add step 10 to the Execution Flow section (after step 9, which is the last current step). Insert it before the "### If ad-hoc task is provided:" subsection:

```markdown
10. **Browser engine resolution**: If spec has `## Browsers`, extract the bullet list of engine names. Run the full test once per listed engine (spawn one agent invocation per engine with `--browser=<engine>`). If section absent, default to `chromium` (single run). Each agent invocation receives the active engine name in the test assignment header.
```

Also add a new usage mode after the existing Mode 7:

```markdown
### Mode 8: Run with specific browser engines
```
/qa:run --project ~/Projects/my-app --spec checkout --browsers
```
Runs the checkout spec using the browser engines defined in the spec's `## Browsers` section.
```

And add an example at the bottom in the Examples section:

```markdown
**Run across browser engines defined in spec:**
```
/qa:run --project ~/Projects/my-app --spec homepage
```
If the homepage spec has a `## Browsers` section listing chromium and webkit, the skill runs the full test suite once per engine.
```

**Part B: Create examples/visual-testing-spec.md**

Create a new example spec file that demonstrates all three new sections working together. Follow the structure of `examples/sample-login-spec.md`. This is a standalone example file (permitted per D-13 which only prohibits combined examples within SPEC-FORMAT.md inline blocks).

Content for `examples/visual-testing-spec.md`:

```markdown
# E-Commerce Homepage

## Overview

The homepage is the primary landing page for the e-commerce store. It features a hero banner, product grid, navigation, and footer. This spec demonstrates the v2 visual testing features: Visual Focus, Design Reference, and Browsers.

## Base URL

http://localhost:3000

## Personas

- unauthenticated user (browsing without login)
- logged-in user (use test@example.com / password123)

## Viewports

- desktop (1280x720)
- mobile (375x667)

## Visual Focus

- design-verification
- layout-integrity

## Design Reference

### desktop
- home-logged-out: .qa/designs/desktop-home-logged-out.png
- home-logged-in: .qa/designs/desktop-home-logged-in.png

### mobile
- home-logged-out: .qa/designs/mobile-home-logged-out.png

## Browsers

- chromium
- webkit

## Test Scenarios

### 1. Homepage Loads Correctly

tags: [smoke, critical]

**Steps:**

1. Navigate to /
2. Verify the page loads without console errors
3. Check that the hero banner is visible
4. Check that the product grid displays at least 4 products

**Expected:**

- Hero banner image loads and is fully visible
- Product grid shows product cards with images, titles, and prices
- Navigation bar is present with all expected links
- Footer is visible at the bottom
- No console errors or warnings
- Page loads within 2 seconds

### 2. Responsive Layout

tags: [regression]
depends_on: 1. Homepage Loads Correctly

**Steps:**

1. At desktop viewport, verify the product grid shows 4 columns
2. At mobile viewport, verify the product grid shows 1-2 columns
3. At mobile viewport, verify the navigation collapses to a hamburger menu

**Expected:**

- Desktop: 4-column product grid, full navigation bar
- Mobile: 1-2 column product grid, hamburger menu
- No horizontal scrolling at any viewport
- All images scale appropriately without distortion

## Edge Cases to Verify

- Very long product names (50+ characters) should truncate or wrap without breaking layout
- Missing product images should show a placeholder, not a broken image icon
- Empty product grid (no products) should show a meaningful empty state

## Things to Watch For

- **Performance**: Largest Contentful Paint should be under 2.5s
- **Visual**: Product card spacing should be consistent across the grid
- **Accessibility**: All images should have alt text, navigation should be keyboard-accessible
- **Cross-browser**: Hero banner rendering may differ between Chromium and WebKit — note any differences

## Known Issues

None currently tracked.
```
  </action>
  <verify>
    <automated>grep -n "Visual Focus" skills/run/SKILL.md && grep -n "Design Reference" skills/run/SKILL.md && grep -n "Browser Engines" skills/run/SKILL.md && grep -n "Active engine" skills/run/SKILL.md && test -f examples/visual-testing-spec.md && grep "## Visual Focus" examples/visual-testing-spec.md && grep "## Design Reference" examples/visual-testing-spec.md && grep "## Browsers" examples/visual-testing-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/run/SKILL.md contains "{If spec has Visual Focus section:}" conditional block
    - skills/run/SKILL.md contains "{If spec has Design Reference section:}" conditional block
    - skills/run/SKILL.md contains "{If spec has Browsers section:}" conditional block
    - skills/run/SKILL.md contains "{If spec has no Browsers section:}" default block with "chromium (default)"
    - skills/run/SKILL.md contains "Active engine for this run" in the Browsers forwarding block
    - skills/run/SKILL.md Execution Flow has step 10 mentioning "Browser engine resolution"
    - examples/visual-testing-spec.md exists as a new file
    - examples/visual-testing-spec.md contains "## Visual Focus" with "design-verification" and "layout-integrity"
    - examples/visual-testing-spec.md contains "## Design Reference" with "### desktop" and "### mobile" subsections
    - examples/visual-testing-spec.md contains "## Browsers" with "chromium" and "webkit"
    - examples/visual-testing-spec.md contains "## Test Scenarios" with at least one scenario
    - Existing agent invocation template blocks ({If environment profile active:}, {If tag filtering active:}, etc.) are unchanged
  </acceptance_criteria>
  <done>skills/run/SKILL.md has three new conditional forwarding blocks in the agent invocation template for Visual Focus, Design Reference, and Browsers. Execution Flow has step 10 for browser engine resolution. A new examples/visual-testing-spec.md demonstrates all three new sections in a realistic spec. Existing invocation template blocks are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase makes zero changes to runtime execution paths, authentication, input handling, or data processing. All deliverables are static Markdown documentation and AI instruction edits.

| Boundary | Description |
|----------|-------------|
| N/A | No trust boundaries affected — pure documentation phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | Design Reference paths in spec files | accept | Image paths are relative project paths, not secrets. Spec files are project-local and gitignored by convention (.qa/ directory). No risk of leaking sensitive data. |
| T-08-02 | Tampering | Spec sections could instruct agent to execute unintended commands | accept | The agent interprets spec sections as test instructions within its existing sandboxed execution model (playwright-cli via Bash). No new tool grants or permission changes. The spec format is read-only input to the agent. |
</threat_model>

<verification>
After all three tasks complete, verify the full phase:

1. `grep -c "### Visual Focus (v2)" examples/SPEC-FORMAT.md` returns 1
2. `grep -c "### Design Reference (v2)" examples/SPEC-FORMAT.md` returns 1
3. `grep -c "### Browsers (v2)" examples/SPEC-FORMAT.md` returns 1
4. `grep "Visual Focus.*Design Reference.*Browsers" examples/SPEC-FORMAT.md` matches in backward compat clause
5. `grep -c "### Visual Focus Trigger" agents/qa-tester.md` returns 1
6. `grep -c "### Accessibility Focus Trigger" agents/qa-tester.md` returns 1 (unchanged)
7. `grep "design-verification" agents/qa-tester.md` matches in Visual Focus Trigger
8. `grep "Active engine for this run" skills/run/SKILL.md` matches in Browser Engines block
9. `test -f examples/visual-testing-spec.md` succeeds
10. Run existing `examples/sample-login-spec.md` through a mental parse -- it has no new sections, so all behavior should be unchanged (backward compatible)
</verification>

<success_criteria>
- SPEC-FORMAT.md documents all three new sections with behavior, examples, and available values
- qa-tester.md has Visual Focus Trigger, Design Reference, and Browsers instruction sections
- skills/run/SKILL.md forwards all three new spec sections to the agent in the invocation template
- A sample spec file demonstrates all three sections working together
- Backward Compatibility clause in SPEC-FORMAT.md lists all three new sections as optional
- Existing Accessibility Focus Trigger in qa-tester.md is unchanged
- Existing invocation template blocks in skills/run/SKILL.md are unchanged
- No methodology is implemented -- only vocabulary, triggers, and placeholders
</success_criteria>

<output>
After completion, create `.planning/phases/08-spec-format-extensions/08-01-SUMMARY.md`
</output>
