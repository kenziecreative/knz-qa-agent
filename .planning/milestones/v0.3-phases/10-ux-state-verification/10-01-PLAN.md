---
phase: 10-ux-state-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/qa-tester.md
autonomous: true
requirements:
  - STATE-01
  - STATE-02

must_haves:
  truths:
    - "Agent can force empty, loading, error, and first-run states into existence via active triggering and verify they render"
    - "Agent falls back to passive DOM probing when active triggering would be destructive"
    - "Agent cleans up route mocks and storage after each state trigger to prevent state bleed"
    - "Agent sweeps interactive components through all 8 states: default, hover, focus, active, disabled, loading, error, success"
  artifacts:
    - path: "agents/qa-tester.md"
      provides: "UX State Verification section header + STATE-01 + STATE-02 methodology"
      contains: "### UX State Verification"
    - path: "agents/qa-tester.md"
      provides: "State Existence subsection"
      contains: "#### State Existence (STATE-01)"
    - path: "agents/qa-tester.md"
      provides: "Interaction State Matrix subsection"
      contains: "#### Interaction State Matrix (STATE-02)"
  key_links:
    - from: "agents/qa-tester.md (Visual Focus Trigger)"
      to: "agents/qa-tester.md (UX State Verification section)"
      via: "ux-states area name triggers this section"
      pattern: "ux-states"
    - from: "agents/qa-tester.md (STATE-01 active triggering)"
      to: "playwright-cli route"
      via: "route interception for empty/loading/error states"
      pattern: "playwright-cli route"
    - from: "agents/qa-tester.md (STATE-02 interaction matrix)"
      to: "agents/qa-tester.md (DESIGN-04 hover/focus pattern)"
      via: "extends the hover-then-eval pattern to 8 states"
      pattern: "playwright-cli hover"
---

<objective>
Add the UX State Verification section header, State Existence (STATE-01), and Interaction State Matrix (STATE-02) methodology to agents/qa-tester.md.

Purpose: This is the first half of the UX state verification methodology. STATE-01 teaches the agent to actively trigger and verify empty/loading/error/first-run states. STATE-02 extends the existing DESIGN-04 hover/focus pattern into a full 8-state interaction sweep per component. Both follow the dual eval+visual-judgment pattern from Phase 9.

Output: agents/qa-tester.md with new `### UX State Verification` section containing two subsections inserted after the Design Verification reporting table (line 1098), before `### Design Reference` (line 1099).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ux-state-verification/10-CONTEXT.md
@.planning/phases/10-ux-state-verification/10-RESEARCH.md
@.planning/phases/10-ux-state-verification/10-PATTERNS.md

<interfaces>
<!-- Key patterns and insertion points the executor needs from agents/qa-tester.md -->

Line 747 — Visual Focus stub note (pending areas list):
```markdown
**Note:** Tier 2 visual methodology for `ux-states`, `layout-integrity`, and `performance-responsive` will be added in Phases 10-12. If a Visual Focus section requests those areas before their phases ship, note in the report: "Visual Focus requested for [areas] — full methodology pending Phase [N]."
```

Lines 749-755 — Design Verification section header (pattern to mirror):
```markdown
### Design Verification

Tier 2 visual design verification — runs when `design-verification` is listed in the spec's `## Visual Focus` section. All five areas use the same dual pattern: `playwright-cli eval` for deterministic DOM/CSS probes (high confidence) + `playwright-cli screenshot` + visual judgment for perceptual quality (medium confidence).

If no `## Design Reference` section was forwarded in the test assignment, skip the Mockup Comparison procedure below and note "no design reference provided — mockup comparison skipped." Continue to Typography Verification through Cross-Page Consistency.

**Security note:** When reading CSS custom properties via `playwright-cli eval`, do not include property names beginning with `--auth`, `--token`, `--key`, or `--secret` in reports.
```

Lines 1076-1098 — Design Verification reporting table (the section this plan inserts AFTER):
```markdown
#### Reporting Design Verification Findings
... (16-row table ending at line 1098)
```

Line 1099 — `### Design Reference` (the section the new content goes BEFORE):
```markdown
### Design Reference
```

Lines 943-968 — DESIGN-04 hover-then-eval pattern (STATE-02 extends this):
```markdown
   a. Identify the target element via `playwright-cli snapshot` (get element ref)
   b. Read default state colors: playwright-cli eval ...
   c. Trigger hover state: `playwright-cli hover <element-ref>`
   d. **Immediately** read hover state colors ...
   e. Compare default vs hover — if colors are identical, the element has no visible hover state.
   f. Repeat for focus state: `playwright-cli click <element-ref>` or Tab to focus, then eval.
```

Security guard pattern (line 927) — MUST carry forward:
```javascript
if (prop.startsWith('--') && !prop.match(/^--(auth|token|key|secret)/)) {
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UX State Verification section header and State Existence (STATE-01)</name>
  <files>agents/qa-tester.md</files>
  <read_first>
    - agents/qa-tester.md (full file — focus on lines 747, 749-755 for header pattern, 1076-1099 for insertion point)
    - .planning/phases/10-ux-state-verification/10-CONTEXT.md (D-01 through D-04, D-17 for decisions)
    - .planning/phases/10-ux-state-verification/10-PATTERNS.md (section header pattern, subsection naming, eval probe format)
    - .planning/phases/10-ux-state-verification/10-RESEARCH.md (Pattern 1: Active State Triggering, Code Examples section)
  </read_first>
  <action>
Insert a new `### UX State Verification` section in agents/qa-tester.md AFTER line 1098 (end of `#### Reporting Design Verification Findings` table) and BEFORE the existing `### Design Reference` section (currently line 1099).

**Section header** (mirror Design Verification header at lines 749-755):

```markdown
### UX State Verification

Tier 2 UX state verification — runs when `ux-states` is listed in the spec's `## Visual Focus` section. All four areas use the same dual pattern: `playwright-cli eval` for deterministic DOM/CSS probes (high confidence) + `playwright-cli screenshot` + visual judgment for perceptual quality (medium confidence).

**Security note:** When reading CSS custom properties via `playwright-cli eval`, do not include property names beginning with `--auth`, `--token`, `--key`, or `--secret` in reports.

**Environment note:** Active state triggering (storage clearing, cookie deletion, route mocking) manipulates application state. Use only in test environments — never against production data.
```

**`#### State Existence (STATE-01)` subsection** — numbered procedural steps following the DESIGN-02/DESIGN-03 pattern:

Write these steps implementing decisions D-01 through D-04:

1. **Active triggering is the default methodology** (per D-01). For each state type, attempt to force the state into existence, then verify it renders correctly. Active triggering catches "state defined but never fires" bugs that passive checks miss.

2. **Empty state** (per D-02): Clear storage and mock empty API response:
   - `playwright-cli run-code "async (page) => { await page.context().clearCookies(); }"` + `playwright-cli localstorage-clear`
   - `playwright-cli route "*/api/*" "{\"body\": \"[]\", \"contentType\": \"application/json\"}"` to return empty arrays
   - Navigate to the page, take screenshot
   - Eval probe for empty state indicators:
   ```
   playwright-cli eval "() => {
     const selectors = [
       '[data-empty]', '[data-state=\"empty\"]', '[data-testid*=\"empty\"]',
       '.empty-state', '.empty', '.no-results', '.no-items', '.no-data',
       '[aria-label*=\"empty\"]', '[aria-label*=\"no items\"]'
     ];
     const listContainers = Array.from(
       document.querySelectorAll('ul, ol, [role=\"list\"], [role=\"grid\"], [role=\"table\"], table tbody')
     ).filter(el => el.children.length === 0 && el.offsetParent !== null);
     return {
       bySelector: selectors.filter(s => document.querySelector(s)),
       emptyListContainers: listContainers.map(el => ({
         tag: el.tagName, id: el.id, className: String(el.className).slice(0, 50)
       }))
     };
   }"
   ```
   - If no empty state indicator found AND list container has 0 children: flag as **High confidence** — "empty state missing: list renders with zero items but no empty state messaging."
   - Cleanup: `playwright-cli route-clear "*/api/*"` (per D-04)

3. **Loading state** (per D-02): Intercept and delay API responses:
   - `playwright-cli route "*/api/*" "{\"delay\": 3000}"` to hold loading state visible
   - Navigate to the page
   - Immediately screenshot and eval for loading indicators:
   ```
   playwright-cli eval "() => {
     const indicators = document.querySelectorAll(
       '[role=\"progressbar\"], [aria-label*=\"loading\"], [aria-busy=\"true\"], [class*=\"spinner\"], [class*=\"skeleton\"], [class*=\"loader\"], [class*=\"loading\"]'
     );
     return { count: indicators.length, found: indicators.length > 0 };
   }"
   ```
   - If no loading indicator found during delayed response: flag as **High confidence** — "loading state missing: API response delayed but no loading indicator visible."
   - Cleanup: `playwright-cli route-clear "*/api/*"` (per D-04)

4. **Error state** (per D-02): Route returning 5xx status:
   - `playwright-cli route "*/api/*" "{\"status\": 500, \"body\": \"{\\\"error\\\": \\\"Internal Server Error\\\"}\"}"` (uses existing error simulation pattern from Phase 3 ERR-01)
   - Navigate to the page
   - Screenshot and eval for error state indicators:
   ```
   playwright-cli eval "() => {
     const errorIndicators = document.querySelectorAll(
       '[role=\"alert\"], [data-error], [data-state=\"error\"], [class*=\"error\"], [class*=\"fail\"], [aria-label*=\"error\"]'
     );
     const hasErrorText = document.body.innerText.match(/something went wrong|error|failed to load|try again|oops/i);
     return { indicators: errorIndicators.length, hasErrorText: !!hasErrorText, found: errorIndicators.length > 0 || !!hasErrorText };
   }"
   ```
   - If no error state found after 5xx response: flag as **High confidence** — "error state missing: API returned 500 but no error messaging visible to user."
   - Cleanup: `playwright-cli route-clear "*/api/*"` (per D-04)

5. **First-run/onboarding state** (per D-02): Clear all storage + cookies to simulate fresh user:
   - `playwright-cli run-code "async (page) => { await page.context().clearCookies(); }"` + `playwright-cli localstorage-clear`
   - Navigate to the page
   - Screenshot and eval for onboarding markers:
   ```
   playwright-cli eval "() => {
     const selectors = [
       '[data-tour]', '[data-onboarding]', '[data-welcome]', '[data-first-run]',
       '.onboarding', '.welcome', '.getting-started', '.first-run',
       '[aria-label*=\"welcome\"]', '[aria-label*=\"get started\"]', '[aria-label*=\"tour\"]'
     ];
     return selectors.map(s => ({
       selector: s,
       found: !!document.querySelector(s),
       visible: (() => {
         const el = document.querySelector(s);
         if (!el) return false;
         const r = el.getBoundingClientRect();
         return r.width > 0 && r.height > 0;
       })()
     })).filter(r => r.found);
   }"
   ```
   - If no onboarding markers found on first visit: note as **observation** — "no first-run/onboarding state detected on fresh session. This may be intentional if the app does not have an onboarding flow."
   - Note: Unlike empty/loading/error, first-run absence is an observation, not a finding — not all apps have onboarding.

6. **Passive DOM probing as fallback** (per D-03): When the spec provides no trigger guidance or when a note in the spec indicates active manipulation would be destructive (e.g., "do not clear storage — app has real user data"), skip active triggering for that state type and use eval-only DOM probing (the eval patterns from steps 2-5 above, without the route/storage manipulation). Note in the report: "passive fallback used — active triggering skipped for [state type] per spec guidance."

7. **State cleanup mandate** (per D-04): After EACH active state trigger (steps 2-5), the route mock or storage change MUST be cleaned up before proceeding to the next state check. This prevents state bleed where e.g. an empty-state route mock is still active when testing loading state.

End with confidence line:
```
Confidence: Missing empty/loading/error state with eval-confirmed absence = High. First-run/onboarding not detected = observation only. Visual quality of state rendering = Medium (screenshot judgment).
```
  </action>
  <verify>
    <automated>grep -c "#### State Existence (STATE-01)" agents/qa-tester.md | grep -q "1" && grep -c "### UX State Verification" agents/qa-tester.md | grep -q "1" && grep "do not include property names beginning with" agents/qa-tester.md | grep -c "UX State" | grep -q "0" || grep -c "Security note" agents/qa-tester.md | grep -q "[2-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - agents/qa-tester.md contains `### UX State Verification` exactly once, positioned after the Design Verification reporting table and before `### Design Reference`
    - agents/qa-tester.md contains `#### State Existence (STATE-01)` exactly once
    - Section header contains "Tier 2 UX state verification" and "ux-states"
    - Section header contains "Security note:" with the `--auth`, `--token`, `--key`, `--secret` exclusion
    - Section header contains "Environment note:" with test-environment-only caveat
    - STATE-01 contains `playwright-cli route` for empty/loading/error state triggering
    - STATE-01 contains `playwright-cli route-clear` cleanup steps after each trigger
    - STATE-01 contains `localstorage-clear` for empty and first-run states
    - STATE-01 contains passive fallback paragraph referencing D-03 rationale
    - STATE-01 ends with a `Confidence:` summary line
    - Eval code blocks use the fenced code format (no language tag) matching Design Verification pattern
    - Numbered procedural steps match the DESIGN-02/DESIGN-03 formatting pattern
  </acceptance_criteria>
  <done>UX State Verification section header and STATE-01 State Existence subsection are inserted in agents/qa-tester.md with active triggering for all four state types, passive fallback, state cleanup mandate, security note, environment caveat, and confidence summary.</done>
</task>

<task type="auto">
  <name>Task 2: Add Interaction State Matrix (STATE-02) subsection</name>
  <files>agents/qa-tester.md</files>
  <read_first>
    - agents/qa-tester.md (read the STATE-01 section just inserted by Task 1 for placement, and lines 943-968 for DESIGN-04 hover-then-eval pattern to extend)
    - .planning/phases/10-ux-state-verification/10-CONTEXT.md (D-05 through D-08 for interaction matrix decisions)
    - .planning/phases/10-ux-state-verification/10-PATTERNS.md (hover/focus eval-after-interaction pattern excerpt)
    - .planning/phases/10-ux-state-verification/10-RESEARCH.md (Pattern 2: Interaction State Matrix Sweep, Pattern 3: Cursor)
  </read_first>
  <action>
Insert `#### Interaction State Matrix (STATE-02)` subsection in agents/qa-tester.md immediately after the STATE-01 `Confidence:` line. Use numbered procedural steps matching the DESIGN-04 pattern.

Write these steps implementing decisions D-05 through D-08:

Opening paragraph: "Systematically sweep each interactive component through its full state matrix. This extends the DESIGN-04 hover/focus verification pattern to eight states per component."

1. **Identify interactive elements** (per D-06): Use `playwright-cli snapshot` to capture the accessibility tree. Identify all elements matching: `button`, `[role="button"]`, `a`, `input`, `select`, `textarea`, `[tabindex]`. If the page has many interactive elements (50+), prioritize: primary action buttons and CTAs first, navigation links second, form inputs third, decorative/tertiary elements only if time allows. The spec can override this prioritization with an explicit component list.

2. **Per-component state sweep** (per D-05, D-07): For each target element, follow this sequence:

   a. **Default** (baseline): Read computed styles at rest:
   ```
   playwright-cli eval "() => {
     const el = document.querySelector('[target-selector]');
     const s = getComputedStyle(el);
     return {
       bg: s.backgroundColor, color: s.color, opacity: s.opacity,
       border: s.borderColor, outline: s.outlineColor,
       cursor: s.cursor, transform: s.transform,
       boxShadow: s.boxShadow, textDecoration: s.textDecoration
     };
   }"
   ```

   b. **Hover**: Trigger hover via `playwright-cli hover <element-ref>`, then **immediately** eval the same properties (per D-05, extending DESIGN-04 hover-then-eval). Compare to default — if all properties are identical, the element has no visible hover state. Flag as **Medium confidence** — "interactive element has no visible hover state change."

   c. **Focus**: Tab to the element (or `playwright-cli click <element-ref>` if Tab order is unreliable), then eval. Check for focus ring or visual change. If no visual change from default: flag as **High confidence** — "interactive element has no visible focus indicator" (accessibility impact).

   d. **Active** (per D-08): Take a screenshot during the click sequence. This is **visual-judgment-only at Medium confidence** — the mousedown state resolves in milliseconds, making eval capture unreliable. Note: "active state assessed via screenshot — eval capture unreliable for momentary mousedown state."

   e. **Disabled**: Query the DOM for disabled variants:
   ```
   playwright-cli eval "() => {
     const disabled = Array.from(
       document.querySelectorAll('[disabled], [aria-disabled=\"true\"]')
     ).filter(el => el.offsetParent !== null);
     return disabled.map(el => {
       const s = getComputedStyle(el);
       return {
         tag: el.tagName, text: el.textContent.trim().slice(0, 40),
         opacity: s.opacity, cursor: s.cursor,
         hasVisualDisabledStyle: parseFloat(s.opacity) < 1 || s.cursor === 'not-allowed' || s.pointerEvents === 'none'
       };
     });
   }"
   ```
   - If a disabled element has full opacity AND cursor is not `not-allowed` AND pointerEvents is not `none`: flag as **Medium confidence** — "disabled element lacks visual disabled styling."

   f. **Loading/Error/Success** (per D-07): If the component has loading, error, or success states exposed via the application flow, navigate to them. For error states, use `playwright-cli route` to intercept the component's API endpoint with a 5xx response (same pattern as STATE-01 error state). For each exposed state, eval computed styles and compare to default. If the component shows no visual differentiation in error/success states: flag as **Medium confidence**.

3. **Report format**: For each component swept, report the states that were verifiable and any missing visual differentiation. Group findings by confidence level.

End with confidence line:
```
Confidence: No visible focus indicator = High (accessibility). Eval-confirmed identical styles across states = High. No visible hover change = Medium. Active state visual judgment = Medium. Disabled element without visual styling = Medium.
```
  </action>
  <verify>
    <automated>grep -c "#### Interaction State Matrix (STATE-02)" agents/qa-tester.md | grep -q "1" && grep "playwright-cli hover" agents/qa-tester.md | wc -l | awk '{if($1>=2) print "PASS"; else print "FAIL"}'</automated>
  </verify>
  <acceptance_criteria>
    - agents/qa-tester.md contains `#### Interaction State Matrix (STATE-02)` exactly once, positioned after STATE-01
    - STATE-02 references `playwright-cli snapshot` for element identification (per D-06)
    - STATE-02 lists the element selector set: `button`, `[role="button"]`, `a`, `input`, `select`, `textarea`, `[tabindex]` (per D-06)
    - STATE-02 contains the 6-step per-component sequence: default, hover, focus, active, disabled, loading/error/success (per D-07)
    - Hover step uses `playwright-cli hover` then immediate eval (extending DESIGN-04 pattern per D-05)
    - Active state step explicitly notes "visual-judgment-only" and "Medium confidence" (per D-08)
    - Disabled state uses `[disabled]` and `aria-disabled` query (per D-07)
    - STATE-02 ends with a `Confidence:` summary line
    - Eval code blocks use fenced code format (no language tag) matching Design Verification pattern
    - Numbered procedural steps follow the DESIGN-04 formatting pattern
  </acceptance_criteria>
  <done>STATE-02 Interaction State Matrix subsection is inserted in agents/qa-tester.md with the full 8-state per-component sweep methodology extending DESIGN-04, element selection via accessibility tree snapshot, and per-state confidence levels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent eval -> browser DOM | eval probes read computed styles from potentially untrusted web content |
| Agent -> application state | Active triggering manipulates storage/cookies/routes in the target application |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | STATE-02 eval probes reading CSS custom properties | mitigate | Carry forward security guard from DESIGN-04: `!prop.match(/^--(auth|token|key|secret)/)` — included in section-level security note |
| T-10-02 | Denial of Service | STATE-01 active triggering clearing storage/cookies against production | mitigate | Environment note in section header: "Use only in test environments — never against production data" |
| T-10-03 | Elevation of Privilege | Route mocks persisting beyond test scope contaminating subsequent checks | mitigate | D-04 state cleanup mandate: explicit `route-clear` step after each state trigger, documented as numbered step 7 |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "### UX State Verification" agents/qa-tester.md` returns 1
2. `grep -c "#### State Existence (STATE-01)" agents/qa-tester.md` returns 1
3. `grep -c "#### Interaction State Matrix (STATE-02)" agents/qa-tester.md` returns 1
4. The new section appears AFTER `#### Reporting Design Verification Findings` table and BEFORE `### Design Reference`
5. Security note with `--auth`, `--token`, `--key`, `--secret` exclusion is present in the new section header
6. Environment note about test-only usage is present
7. `route-clear` cleanup steps appear after each active trigger in STATE-01
</verification>

<success_criteria>
agents/qa-tester.md contains a complete `### UX State Verification` section with STATE-01 (active triggering for 4 state types + passive fallback + cleanup mandate) and STATE-02 (8-state interaction matrix extending DESIGN-04) inserted at the correct location, following the dual eval+visual pattern, with security and environment notes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ux-state-verification/10-01-SUMMARY.md`
</output>
