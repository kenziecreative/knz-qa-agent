---
phase: 05-spec-format-v2
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - README.md
  - skills/qa-init.md
autonomous: true

must_haves:
  truths:
    - "README documents all v2 spec features with examples"
    - "qa-init smoke-test template demonstrates v2 format"
    - "New users can understand and use v2 features from README alone"
  artifacts:
    - path: "README.md"
      provides: "Complete documentation of v2 spec format, --tag, --env, --a11y-depth"
      contains: "--tag"
    - path: "skills/qa-init.md"
      provides: "Updated init templates with v2 format sections"
      contains: "tags:"
  key_links:
    - from: "README.md"
      to: "skills/qa-run.md"
      via: "README documents --tag and --env flags that qa-run implements"
      pattern: "--tag|--env"
    - from: "README.md"
      to: "skills/qa-gen.md"
      via: "README documents --a11y-depth flag that qa-gen implements"
      pattern: "a11y-depth"
---

<objective>
Update README and init templates to document all v2 spec format features.

Purpose: Users need comprehensive documentation to understand and adopt v2 features. The README is the primary entry point; init templates set good defaults for new projects.
Output: README.md with full v2 documentation. qa-init.md templates updated with v2 format sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-spec-format-v2/05-CONTEXT.md

@README.md
@skills/qa-init.md

# Need to reference what was built in prior plans
@.planning/phases/05-spec-format-v2/05-01-SUMMARY.md
@.planning/phases/05-spec-format-v2/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README with v2 spec format documentation</name>
  <files>README.md</files>
  <action>
Update `README.md` to document all v2 features. Preserve existing structure and content — add v2 information alongside existing material.

**1. Update the `/qa:run` section** to add new flag examples:

After existing examples, add:
```bash
# Run only smoke-tagged scenarios
/qa:run --project ~/Projects/my-app --tag smoke

# Run against staging environment
/qa:run --project ~/Projects/my-app --spec checkout --env staging

# Combine: critical tests on staging
/qa:run --project ~/Projects/my-app --tag critical --env staging
```

**2. Update the `/qa:gen` section** to add --a11y-depth example:

```bash
# Generate with deep accessibility testing
/qa:gen dashboard --a11y-depth deep "Admin dashboard with tables and filters"
```

**3. Update the "Spec Format" section** significantly:

The current section shows a basic v1 example. Expand it to show v2 features. Keep the v1 example as "Basic Spec" and add a "v2 Features" subsection showing:

- **Tags**: `tags: [smoke, critical]` on scenarios — with explanation of standard tags (smoke, critical, regression, a11y)
- **Dependencies**: `depends_on: Successful Login` — with explanation that failed dependencies cause skips
- **Data-Driven Scenarios**: Show a scenario with `## Data Sets` containing multiple variants
- **Environment Profiles**: Show `## Environments` section with local/staging profiles
- **Accessibility Focus**: Show `## Accessibility Focus` section and explain Tier 1 vs Tier 2 trigger

Keep the v1 spec example intact as it serves as a minimal/backward-compatible example.

**4. Update the "Accessibility" section:**

Change "Planned (Tier 2)" to "Tier 2 (activated via spec)" since it's now implemented. Explain that adding `## Accessibility Focus` to a spec activates Tier 2 depth, or using `--a11y-depth deep` with qa:gen.

**5. Update Version History:**

Under the 0.2.0 entry, update the "Spec format v2" bullet to indicate it's done (not just planned):
```
- Spec format v2 (dependencies, data-driven, tags/filtering, environment profiles, accessibility depth)
```

**6. Do NOT change:**
- Installation section
- Project Structure section
- Requirements section
- License section
- Any section not related to v2 features
  </action>
  <verify>
Read `README.md` and confirm:
- /qa:run section shows --tag and --env examples
- /qa:gen section shows --a11y-depth example
- Spec Format section documents tags, dependencies, data-driven, environments, accessibility focus
- Accessibility section updated to reflect Tier 2 as implemented
- Version history updated
- Existing content preserved
  </verify>
  <done>
README comprehensively documents all v2 spec features with examples. A new user reading only the README can understand and use tags, dependencies, data-driven scenarios, environment profiles, and accessibility depth controls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update qa-init templates with v2 format sections</name>
  <files>skills/qa-init.md</files>
  <action>
Update `skills/qa-init.md` to generate v2-aware templates when initializing a new project.

**1. Update the Template: README.md section:**

In the "Writing Specs" bullet list, add new items:
- **Tags**: Label scenarios with `tags: [smoke, critical, regression, a11y]` for selective execution
- **Dependencies**: Use `depends_on: Scenario Name` when scenarios require prior scenarios to pass
- **Environments**: Define `## Environments` with profiles for local/staging/production
- **Accessibility Focus**: Add `## Accessibility Focus` section to trigger deep a11y testing

**2. Update the Template: smoke-test.md section:**

Update the generated smoke-test template to demonstrate v2 features lightly:

- Add `tags: [smoke]` to each scenario
- Add a simple `## Environments` section with a local profile template:
  ```
  ## Environments
  ### local
  - **base_url**: http://localhost:3000
  ```
- Do NOT add dependencies or data-driven sections to the smoke test (too simple to need them)
- Do NOT add Accessibility Focus (smoke test is quick health check, not deep a11y)

Keep the smoke test simple — it should introduce v2 syntax without overwhelming new users.

**3. Update "After Initialization" message:**

Add bullet point:
- Specs support tags, dependencies, data-driven scenarios, environments, and accessibility depth — see the [Spec Format docs](examples/SPEC-FORMAT.md) for full reference
  </action>
  <verify>
Read `skills/qa-init.md` and confirm:
- Template README mentions tags, dependencies, environments, accessibility focus
- smoke-test.md template includes tags: [smoke] on scenarios
- smoke-test.md template includes basic Environments section
- After Initialization message mentions v2 features
- Templates remain simple and approachable for new users
  </verify>
  <done>
qa-init generates v2-aware templates that introduce new spec features without overwhelming new users. The README template references all v2 capabilities; the smoke-test template uses tags and environments as gentle introduction.
  </done>
</task>

</tasks>

<verification>
- `grep -c "\-\-tag" README.md` returns > 0
- `grep -c "\-\-env" README.md` returns > 0
- `grep -c "a11y-depth" README.md` returns > 0
- `grep -c "Accessibility Focus" README.md` returns > 0
- `grep -c "tags:" skills/qa-init.md` returns > 0
- `grep -c "Environments" skills/qa-init.md` returns > 0
</verification>

<success_criteria>
1. README documents --tag, --env, and --a11y-depth flags with examples
2. README Spec Format section covers all five v2 features
3. README Accessibility section reflects Tier 2 as implemented (not planned)
4. qa-init templates introduce v2 syntax gently
5. All existing README content preserved
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-format-v2/05-03-SUMMARY.md`
</output>
