---
phase: 13-usage-guidance
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - skills/guide/SKILL.md
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - INFRA-05
must_haves:
  truths:
    - "/qa:guide reads project state from disk and returns context-aware guidance on which QA command to use next"
    - "/qa:guide reads relevant sections of docs/ORCHESTRATION.md to inform its guidance per D-07"
    - "/qa:guide output is a short narrative (2-4 bullet recommendations) not a checklist or table"
    - "README.md lists /qa:guide alongside the other skills"
    - "CLAUDE.md references docs/ORCHESTRATION.md so AI agents know it exists"
  artifacts:
    - path: "skills/guide/SKILL.md"
      provides: "Context-aware usage guidance skill"
      min_lines: 80
      contains: "name: qa:guide"
    - path: "README.md"
      provides: "Updated skill listing with /qa:guide"
      contains: "/qa:guide"
    - path: "CLAUDE.md"
      provides: "Updated project structure with docs/ directory"
      contains: "ORCHESTRATION.md"
  key_links:
    - from: "skills/guide/SKILL.md"
      to: "docs/ORCHESTRATION.md"
      via: "reads ORCHESTRATION.md to ground recommendations"
      pattern: "ORCHESTRATION.md"
    - from: "skills/guide/SKILL.md"
      to: ".qa/"
      via: "inspects project state files"
      pattern: "\\.qa/"
    - from: "README.md"
      to: "docs/ORCHESTRATION.md"
      via: "pointer line"
      pattern: "ORCHESTRATION.md"
    - from: "CLAUDE.md"
      to: "docs/ORCHESTRATION.md"
      via: "project structure entry"
      pattern: "ORCHESTRATION.md"
---

<objective>
Create the /qa:guide skill and update README.md + CLAUDE.md for discoverability. The guide skill reads project QA state from disk plus docs/ORCHESTRATION.md to produce short, actionable guidance on what QA command to run next.

Purpose: INFRA-05 requires a context-aware skill. D-10/D-11/D-12 require discoverability updates to README and CLAUDE.md.

Output: skills/guide/SKILL.md, updated README.md, updated CLAUDE.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-usage-guidance/13-CONTEXT.md
@.planning/phases/13-usage-guidance/13-PATTERNS.md
@.planning/phases/13-usage-guidance/13-01-SUMMARY.md

<interfaces>
<!-- Exact patterns from analog files that the executor must follow -->

From skills/report/SKILL.md (frontmatter — file-read-only skill):
```yaml
---
name: qa:report
description: Generate a cross-session QA report from persisted reports and history, including trend analysis and optional GitHub issue creation
argument-hint: "--project <path> --format <format> --since <date-or-duration> --create-issues"
allowed-tools: Read, Write, Bash, Grep, Glob
---
```

From skills/report/SKILL.md (argument table):
```markdown
## Arguments

| Argument | Required | Default | Description |
|----------|----------|---------|-------------|
| `--project <path>` | Yes | — | Path to project containing `.qa/` directory |
```

From skills/report/SKILL.md (cross-session capability):
```markdown
## Cross-Session Capability

This skill reads entirely from disk. It does not need prior conversation context.
```

From skills/report/SKILL.md (files read table):
```markdown
## Files Read

| File | Purpose |
|------|---------|
| `.qa/reports/{most-recent}.md` | Latest run results — primary data source |
```

From hooks/session-start.md (state inspection commands):
```bash
ls .qa/ 2>/dev/null
ls -t .qa/reports/ 2>/dev/null | head -1
cat .qa/.monitor-alert 2>/dev/null
```

From README.md (skill listing pattern, line ~28):
```markdown
### `/qa:run` - Full Spec Execution

Run comprehensive QA tests from spec files or ad-hoc descriptions.

```bash
# Run a specific spec
/qa:run --project ~/Projects/my-app --spec login
```
```

From README.md (project structure, line ~362):
```markdown
├── monitor/SKILL.md         # /qa:monitor — recurring smoke tests via /loop
└── report/SKILL.md          # /qa:report — cross-session QA reports with trends
```

From CLAUDE.md (project structure, line ~9):
```
skills/
├── run/SKILL.md         # /qa:run — execute tests from specs or ad-hoc
...
└── report/SKILL.md      # /qa:report — cross-session QA reports with trends
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skills/guide/SKILL.md</name>
  <files>skills/guide/SKILL.md</files>
  <read_first>
    - skills/report/SKILL.md (exact analog — file-read-only skill pattern, frontmatter, argument table, cross-session capability, files-read table)
    - skills/init/SKILL.md (second analog — simple file-read-only skill)
    - hooks/session-start.md (state inspection bash commands — same 4 of 5 checks /qa:guide needs)
    - docs/ORCHESTRATION.md (the document this skill reads from per D-07 — created in Plan 01)
    - .planning/phases/13-usage-guidance/13-CONTEXT.md (D-04 through D-07 for skill behavior)
    - .planning/phases/13-usage-guidance/13-PATTERNS.md (skill frontmatter convention, state-inspection pattern, graceful degradation pattern, output format pattern)
  </read_first>
  <action>
Create `skills/guide/` directory and `skills/guide/SKILL.md`.

**Frontmatter (per D-04 — no Agent tool, matches report/init pattern):**
```yaml
---
name: qa:guide
description: Inspect project QA state and recommend the next action
argument-hint: "--project <path>"
allowed-tools: Read, Write, Bash, Grep, Glob
---
```

**Document structure (follow skills/report/SKILL.md structure):**

```
# /qa:guide

Opening paragraph (one sentence per PATTERNS.md): "Read the current project's QA state from disk and return context-aware guidance on which QA command to use next — works without any prior conversation context."

## Arguments

| Argument | Required | Default | Description |
|----------|----------|---------|-------------|
| `--project <path>` | Yes | — | Path to project containing `.qa/` directory (or where one should be created) |

## Usage

```
/qa:guide --project ~/Projects/my-app
/qa:guide --project .
```

## Purpose

Reads from `.qa/` state files and `docs/ORCHESTRATION.md` to produce a short, actionable recommendation. Useful when an orchestrating agent or developer is unsure which QA command to run next.

## Execution Flow

### Step 1: Parse Arguments

Extract `--project` path. Default to current directory if not specified.

### Step 2: Read ORCHESTRATION.md

Read the orchestration reference to ground recommendations in documented guidance (per D-07):

```bash
cat {plugin-root}/docs/ORCHESTRATION.md 2>/dev/null
```

Note: {plugin-root} is the path to the qa-agent plugin installation, not the --project path. Use the skill's own location to find it.

This is the reference document — use it to inform which skill to recommend based on the project state discovered in Step 3.

### Step 3: Inspect Project State

Run all five state checks from D-05. Use `2>/dev/null` on every command to silence errors when files don't exist.

**Check 1: .qa/ directory existence**
```bash
ls {project}/.qa/ 2>/dev/null
```
If not found → recommend `/qa:init {project}`

**Check 2: Spec file count**
```bash
ls {project}/.qa/*.md 2>/dev/null | wc -l
```
If 0 specs (but .qa/ exists) → recommend `/qa:gen`

**Check 3: Report count**
```bash
ls {project}/.qa/reports/*.md 2>/dev/null | wc -l
```
If specs exist but 0 reports → recommend `/qa:run`

**Check 4: HISTORY.md line count**
```bash
wc -l < {project}/.qa/HISTORY.md 2>/dev/null
```
If HISTORY.md has content → note trend data is available, mention `/qa:report` for analysis

**Check 5: Monitor alert presence**
```bash
cat {project}/.qa/.monitor-alert 2>/dev/null
```
If alert file exists → surface the alert prominently, recommend `/qa:run` to investigate or `/qa:report` for context

### Step 4: Compose Guidance

Output format is a short narrative with 2-4 bullet recommendations (per D-06). NOT a checklist or decision tree.

**Priority ordering (first applicable wins as primary recommendation):**
1. Monitor alert present → lead with the alert, recommend investigation
2. No .qa/ directory → recommend /qa:init
3. No spec files → recommend /qa:gen
4. Specs exist but no reports → recommend /qa:run
5. Reports exist → summarize state, suggest /qa:report for trends or /qa:monitor for ongoing checks

**Output style (model on hooks/session-start.md terse style):**

Example when .qa/ doesn't exist:
```
QA not initialized for this project.

- Run `/qa:init {project}` to scaffold the .qa/ directory
- Then `/qa:gen` to create test specs from your feature descriptions
```

Example when specs exist but no reports:
```
QA initialized with {N} spec(s), but no test runs yet.

- Run `/qa:run --project {project} --url <your-url>` to execute your specs
- After running, `/qa:report --project {project}` will show results and trends
```

Example when everything exists:
```
QA active: {N} specs, {M} reports, trend data available.

- Run `/qa:report --project {project}` to review trends and recurring issues
- Run `/qa:run --project {project}` to re-test with latest code changes
- Consider `/qa:monitor --project {project} --url <url>` for continuous regression detection
```

Example with monitor alert:
```
MONITORING ALERT: {alert content}

- Run `/qa:run --project {project}` to investigate the regression
- Run `/qa:report --project {project}` for full trend context
- The alert was written by /qa:monitor — a previously passing scenario is now failing
```

### Step 5: Stop

Output the guidance and stop. Do not run any tests, generate any files, or spawn any agents. This skill only reads and recommends.

## Cross-Session Capability

This skill reads entirely from disk. It does not need prior conversation context. You can run `/qa:guide` in a brand new session and get an accurate recommendation based on the current state of `.qa/`.

## Files Read

| File | Purpose |
|------|---------|
| `docs/ORCHESTRATION.md` | Reference document for grounding recommendations |
| `{project}/.qa/` | Directory existence check — QA initialized? |
| `{project}/.qa/*.md` | Spec file count — tests defined? |
| `{project}/.qa/reports/*.md` | Report count — tests have been run? |
| `{project}/.qa/HISTORY.md` | History line count — trend data available? |
| `{project}/.qa/.monitor-alert` | Alert file — regression detected by monitor? |
```

The skill should be approximately 100-150 lines following the report/SKILL.md structural conventions.
  </action>
  <verify>
    <automated>test -f skills/guide/SKILL.md && echo "EXISTS" && wc -l < skills/guide/SKILL.md && grep -c "allowed-tools" skills/guide/SKILL.md && grep "allowed-tools" skills/guide/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/guide/SKILL.md exists with >= 80 lines
    - Frontmatter contains `name: qa:guide`
    - Frontmatter contains `allowed-tools: Read, Write, Bash, Grep, Glob` (NO `Agent` tool)
    - Frontmatter contains `argument-hint: "--project <path>"`
    - File contains `## Arguments` with a table including `--project <path>`
    - File contains `## Execution Flow` with Step 1 through Step 5
    - Step 3 contains all five state checks: `.qa/` existence, spec count (`*.md`), report count, HISTORY.md line count, `.monitor-alert` presence
    - Step 2 contains reference to reading `docs/ORCHESTRATION.md`
    - File contains `## Cross-Session Capability` section
    - File contains `## Files Read` table listing all 6 files
    - grep "Agent" skills/guide/SKILL.md returns no matches in the allowed-tools line (file-read-only skill per D-04)
    - The word "agent" may appear in prose (e.g., "orchestrating agent") but NOT in frontmatter allowed-tools
  </acceptance_criteria>
  <done>skills/guide/SKILL.md exists as a file-read-only skill that inspects .qa/ state, reads ORCHESTRATION.md, and outputs 2-4 bullet narrative recommendations. It follows the report/SKILL.md pattern exactly for frontmatter, argument table, execution flow, cross-session capability, and files-read table.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md and CLAUDE.md for discoverability</name>
  <files>README.md, CLAUDE.md</files>
  <read_first>
    - README.md (full file — need current skill listing pattern at lines 28-123, project structure at lines 362-386, development notes at lines 394-399)
    - CLAUDE.md (full file — need current project structure at lines 9-30)
    - .planning/phases/13-usage-guidance/13-CONTEXT.md (D-10 through D-13)
    - .planning/phases/13-usage-guidance/13-PATTERNS.md (README.md and CLAUDE.md pattern assignments)
  </read_first>
  <action>
**README.md changes (three insertions):**

1. **Add /qa:guide skill entry** in the Skills section (after `/qa:report` section, before the `## Spec Format` section). Follow the exact pattern from existing entries:

```markdown
### `/qa:guide` - Usage Guidance

Get context-aware recommendations on which QA command to run next, based on current project state.

```bash
# Get guidance for a project
/qa:guide --project ~/Projects/my-app
/qa:guide --project .
```
```

2. **Add guide/SKILL.md to Project Structure** listing. In the skills directory tree (around line 375), add a line for guide between monitor and report (alphabetically) or after report (as the last entry). Use the same inline comment style:

```
│   ├── guide/SKILL.md          # /qa:guide — context-aware usage guidance
```

Also add the docs/ directory entry in the project structure tree:

```
├── docs/
│   └── ORCHESTRATION.md     # Orchestration guide for AI agents and developers
```

3. **Add ORCHESTRATION.md pointer** in the Development Notes section (around line 396). Add a single bullet line per D-10:

```markdown
- See [docs/ORCHESTRATION.md](docs/ORCHESTRATION.md) for orchestration agent usage and AI integration patterns
```

4. **Update the plugin description** at line 7 that says "it adds `/qa:run`, `/qa:check`, `/qa:gen`, `/qa:init`, `/qa:monitor`, and `/qa:report` commands" — add `/qa:guide` to this list.

5. **Update version history** — under the v0.2.0 entry, this is v0.3 work, so either add a v0.3.0 entry or note usage guidance under existing structure. Since v0.3 is the current milestone, add:

```markdown
- **0.3.0** (in progress) - Visual & UX Design Verification
  - Usage guidance: orchestration reference docs and `/qa:guide` skill
```

**CLAUDE.md changes (two insertions):**

1. **Add docs/ directory to Project Structure** block. After the `examples/` line and before `LEARNINGS.md`, add:

```
docs/
└── ORCHESTRATION.md     # AI agent orchestration reference — how to invoke and integrate the plugin
```

2. **Add guide to skills listing** in the Project Structure block. After the report line:

```
├── guide/SKILL.md       # /qa:guide — context-aware usage guidance
```

3. **Update the skill list** in the "What This Is" section (line 7) that says "it adds `/qa:run`, `/qa:check`, `/qa:gen`, `/qa:init`, `/qa:monitor`, and `/qa:report` commands" — add `/qa:guide`.

4. **Update "Skills that spawn the agent" line** (around line 87) to include guide in the non-spawning list:

Change: `Skills that spawn the agent: run, check, gen, monitor. Skills that don't: init, report.`
To: `Skills that spawn the agent: run, check, gen, monitor. Skills that don't: init, report, guide.`

No other changes to CLAUDE.md per D-13 (no hook changes).
  </action>
  <verify>
    <automated>grep "qa:guide" README.md && grep "ORCHESTRATION.md" README.md && grep "qa:guide" CLAUDE.md && grep "ORCHESTRATION.md" CLAUDE.md && grep "guide" CLAUDE.md | grep -i "skill"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `### `/qa:guide` - Usage Guidance` section with bash usage examples
    - README.md contains `guide/SKILL.md` in the project structure tree
    - README.md contains `ORCHESTRATION.md` in at least two places (project structure + development notes pointer)
    - README.md development notes section contains a line linking to docs/ORCHESTRATION.md
    - README.md "What This Is" paragraph includes `/qa:guide` in the skill list
    - CLAUDE.md contains `ORCHESTRATION.md` in the project structure block
    - CLAUDE.md contains `guide/SKILL.md` in the skills listing
    - CLAUDE.md "What This Is" paragraph includes `/qa:guide`
    - CLAUDE.md "Skills that don't" line includes `guide` (e.g., "init, report, guide")
    - No changes to hooks/hooks.json or hooks/session-start.md (per D-13)
  </acceptance_criteria>
  <done>README.md lists /qa:guide alongside the other 6 skills with usage examples, includes guide/SKILL.md and docs/ORCHESTRATION.md in project structure, and has a pointer to ORCHESTRATION.md in development notes. CLAUDE.md references docs/ORCHESTRATION.md and guide/SKILL.md in project structure, lists /qa:guide in the skill enumeration, and includes guide in the non-agent-spawning skill list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill file-read scope | /qa:guide reads from .qa/ directory and docs/ORCHESTRATION.md — no user input processing beyond --project path |
| Documentation content | Static markdown in README.md, CLAUDE.md — no executable code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | I (Information Disclosure) | skills/guide/SKILL.md | accept | Skill reads only .qa/ directory contents (specs, reports, alerts) and ORCHESTRATION.md. No secrets read — .qa/env.local is not in the files-read list. Low risk. |
| T-13-04 | T (Tampering) | README.md, CLAUDE.md | accept | Version-controlled files. Changes visible in git diff. No runtime execution — static documentation. |
| T-13-05 | S (Spoofing) | skills/guide/SKILL.md | accept | Skill does not authenticate or authorize. It reads local files and produces text output. No spoofing vector — same trust as any file-read operation. |
</threat_model>

<verification>
- skills/guide/SKILL.md exists as a file-read-only skill (no Agent in allowed-tools)
- Skill inspects all 5 state items from D-05
- Skill reads ORCHESTRATION.md per D-07
- Skill outputs short narrative (not checklist) per D-06
- README.md mentions /qa:guide in skills listing and project structure
- README.md has ORCHESTRATION.md pointer in development notes
- CLAUDE.md has docs/ directory and ORCHESTRATION.md in project structure
- No changes to hooks per D-13
</verification>

<success_criteria>
Running `/qa:guide --project <path>` produces a short 2-4 bullet narrative recommending the next QA action based on current .qa/ state. README.md and CLAUDE.md both reference /qa:guide and docs/ORCHESTRATION.md for discoverability.
</success_criteria>

<output>
After completion, create `.planning/phases/13-usage-guidance/13-02-SUMMARY.md`
</output>
