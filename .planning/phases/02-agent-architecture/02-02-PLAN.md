---
phase: 02-agent-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/qa-run.md
  - skills/qa-report.md
  - skills/qa-check.md
autonomous: true

must_haves:
  truths:
    - "Every finding in a QA report includes a confidence level (high/medium/low)"
    - "Reports group findings by confidence: Definite Issues, Likely Issues, Possible Issues"
    - "All confidence levels are shown — no filtering"
  artifacts:
    - path: "skills/qa-run.md"
      provides: "Agent prompt requiring confidence per finding"
      contains: "confidence"
    - path: "skills/qa-report.md"
      provides: "Report template with confidence grouping"
      contains: "Definite Issues"
    - path: "skills/qa-check.md"
      provides: "Phase check with confidence in findings"
      contains: "confidence"
  key_links:
    - from: "skills/qa-run.md"
      to: "agents/qa-tester.md"
      via: "agent prompt includes confidence requirement"
      pattern: "confidence.*(high|medium|low)"
    - from: "skills/qa-report.md"
      to: "skills/qa-run.md"
      via: "report consumes findings that include confidence"
      pattern: "Definite Issues|Likely Issues|Possible Issues"
---

<objective>
Add structured confidence levels to all QA output — findings, reports, and phase checks.

Purpose: ARCH-06 requires every finding to include a confidence level. The agent already mentions confidence informally in its instructions, but the skills that invoke it and format output don't enforce structured confidence. This plan updates qa-run, qa-report, and qa-check to require and display confidence-grouped findings.

Output: Updated skills with confidence = certainty x severity, findings grouped as Definite/Likely/Possible Issues.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-agent-architecture/02-CONTEXT.md
@skills/qa-run.md
@skills/qa-report.md
@skills/qa-check.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update qa-run agent prompt to require confidence per finding</name>
  <files>skills/qa-run.md</files>
  <action>
In skills/qa-run.md, update the "Agent Invocation" section's prompt template. In the "## Instructions" block within the prompt, add after "Report findings with full context":

```
- Assign a confidence level to every finding:
  - **High**: Definitely real AND impactful (certainty x severity)
  - **Medium**: Likely real but impact uncertain, OR definitely real but low severity
  - **Low**: Possible issue, needs further investigation or human judgment
- Group findings in your summary by confidence: "Definite Issues" (high), "Likely Issues" (medium), "Possible Issues" (low)
- Include a brief rationale for each confidence assignment
- Show ALL findings — do not filter out low-confidence items
```

Update the "## Output" section to reflect the new structure. Replace the existing summary bullet list with:

```
- Final summary with:
  - Overall result (Pass / Fail / Pass with Concerns)
  - Definite Issues (high confidence) — these need immediate attention
  - Likely Issues (medium confidence) — should be investigated
  - Possible Issues (low confidence) — worth noting for future review
  - Observations (things that worked but are worth noting)
  - Recommendations (if any)
```
  </action>
  <verify>
- `grep "Definite Issues" skills/qa-run.md` returns matches
- `grep "confidence" skills/qa-run.md` returns 3+ matches
- `grep "certainty.*severity\|certainty x severity" skills/qa-run.md` returns a match
  </verify>
  <done>qa-run agent prompt requires confidence per finding with high/medium/low levels, groups output as Definite/Likely/Possible Issues, and includes rationale requirement.</done>
</task>

<task type="auto">
  <name>Task 2: Update qa-report and qa-check for confidence-grouped output</name>
  <files>skills/qa-report.md, skills/qa-check.md</files>
  <action>
**qa-report.md changes:**

In the "## Report Sections" area, update "### Detailed Findings" to organize by confidence:

Replace the current "### Issues to Address" section with:

```
### Definite Issues (High Confidence)
Issues that are certainly real and impactful. These need immediate attention.

For each issue:
- Confidence: High (rationale)
- Severity (Critical / Major / Minor)
- Description
- Where found (spec, scenario)
- Evidence (screenshots, console errors, network failures)
- Suggested next steps

### Likely Issues (Medium Confidence)
Issues that are probably real but need investigation to confirm impact.

(Same structure as above)

### Possible Issues (Low Confidence)
Observations that might indicate problems. Worth noting for future review.

(Same structure as above)
```

Update the example Markdown output at the bottom to replace the "Critical Issues" section with "Definite Issues" grouping, and add Likely/Possible sections. Keep the example concise — just show the Definite section with one example issue that includes a confidence rationale.

In the JSON format example, add a `"confidence"` field to each issue object: `"confidence": "high"` with `"confidence_rationale": "..."`.

**qa-check.md changes:**

In the "### 4. Report Results" section, update all three output templates (passed, failed, partial):

For the failed template, change "Issues Found:" to group by confidence:

```
Definite Issues:
1. {Issue description}
   - Confidence: High — {rationale}
   - Evidence: {what was observed}
   - Severity: {Blocker / Major / Minor}

Likely Issues:
(if any)

Possible Issues:
(if any)
```

For the partial pass template, rename "Concerns:" to "Possible Issues:" and add confidence labels.

For the passed template, no structural change needed (there are no issues to group), but add a note: "Confidence: High — all deliverables verified with evidence."
  </action>
  <verify>
- `grep "Definite Issues" skills/qa-report.md` returns matches
- `grep "Likely Issues" skills/qa-report.md` returns matches
- `grep "Possible Issues" skills/qa-report.md` returns matches
- `grep "confidence" skills/qa-report.md` returns 3+ matches
- `grep "Definite Issues\|confidence" skills/qa-check.md` returns matches
  </verify>
  <done>qa-report groups findings by confidence (Definite/Likely/Possible) in both markdown and JSON formats. qa-check uses confidence grouping in its pass/fail/partial templates. All confidence levels shown, none filtered.</done>
</task>

</tasks>

<verification>
- All three skills reference confidence levels: `grep -l "confidence" skills/qa-run.md skills/qa-report.md skills/qa-check.md` returns all three files
- Confidence grouping present: `grep -l "Definite Issues" skills/qa-run.md skills/qa-report.md skills/qa-check.md` returns all three
- No filtering language: `grep -c "filter\|hide\|suppress" skills/qa-run.md skills/qa-report.md skills/qa-check.md` should return 0 for each
</verification>

<success_criteria>
- ARCH-06: Every finding in a report includes a confidence level (high/medium/low)
- Reports group by confidence: Definite Issues, Likely Issues, Possible Issues
- Confidence = certainty x severity (documented)
- All confidence levels shown — no filtering
- JSON format includes confidence field per issue
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-architecture/02-02-SUMMARY.md`
</output>
