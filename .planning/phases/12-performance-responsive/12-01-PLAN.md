---
phase: 12-performance-responsive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [agents/qa-tester.md]
autonomous: true
requirements: [PERF-01, PERF-02]
must_haves:
  truths:
    - "Agent measures Lab LCP, Lab CLS, and Lab INP using raw PerformanceObserver via playwright-cli eval/run-code"
    - "Agent attributes poor Web Vitals scores to specific elements using entry.element and entry.sources"
    - "Agent compares getComputedStyle properties across browser engines and flags divergences from Chromium baseline"
    - "Agent exempts intentional cross-browser divergences (native form controls, scrollbars, system fonts, -webkit- properties)"
    - "All Web Vitals findings are labeled as lab-environment values, not field/CrUX data"
  artifacts:
    - path: "agents/qa-tester.md"
      provides: "Performance & Responsive section header, Core Web Vitals subsection, Cross-Browser Rendering subsection"
      contains: "### Performance & Responsive"
    - path: "agents/qa-tester.md"
      provides: "PERF-01 Core Web Vitals methodology with PerformanceObserver injection"
      contains: "#### Core Web Vitals (PERF-01)"
    - path: "agents/qa-tester.md"
      provides: "PERF-02 Cross-Browser Rendering methodology with getComputedStyle diff"
      contains: "#### Cross-Browser Rendering Comparison (PERF-02)"
  key_links:
    - from: "agents/qa-tester.md (Performance & Responsive section)"
      to: "agents/qa-tester.md (Visual Focus trigger at line 743)"
      via: "performance-responsive area name triggering Tier 2"
      pattern: "performance-responsive"
    - from: "agents/qa-tester.md (PERF-02)"
      to: "agents/qa-tester.md (Browsers section at line 1819)"
      via: "engine-tagged findings cross-reference"
      pattern: "cross-browser"
---

<objective>
Add the Performance & Responsive section header, Core Web Vitals measurement methodology (PERF-01), and Cross-Browser Rendering Comparison methodology (PERF-02) to qa-tester.md. Also remove the "pending Phase 12" stub note and extend the Confidence Summary Table with PERF-01/PERF-02 entries.

Purpose: Enable the agent to measure Web Vitals via raw PerformanceObserver and compare rendered CSS across browser engines, completing half of the performance-responsive Tier 2 methodology.
Output: Updated agents/qa-tester.md with new methodology section (first half)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-performance-responsive/12-CONTEXT.md
@.planning/phases/12-performance-responsive/12-RESEARCH.md
@.planning/phases/12-performance-responsive/12-PATTERNS.md

<interfaces>
<!-- Key insertion points and patterns the executor needs -->

From agents/qa-tester.md line 743 (Visual Focus area list — DO NOT MODIFY, just cross-reference):
```
  - `performance-responsive` — Core Web Vitals, cross-browser rendering, viewport sweep (Phase 12)
```

From agents/qa-tester.md lines 747-748 (DELETE these lines — the pending note):
```
**Note:** Tier 2 visual methodology for `performance-responsive` will be added in Phase 12. If a Visual Focus section requests that area before Phase 12 ships, note in the report: "Visual Focus requested for performance-responsive — full methodology pending Phase 12."
```

From agents/qa-tester.md lines 349-372 (Confidence Summary Table — APPEND PERF entries after line 372):
```markdown
### Reporting Accessibility Findings

| Finding type | Confidence |
| --- | --- |
| Focus not moving into modal | High |
...
| Focus not moving to first error after submission | Medium |
```

From agents/qa-tester.md line 1802 (INSERT new section AFTER this line, BEFORE line 1803 `### Design Reference`):
```
| Visual alignment/spacing judgment from screenshot | Medium |
```

From agents/qa-tester.md lines 1421-1427 (Section header pattern to follow):
```markdown
### Layout & Content Integrity

Tier 2 layout and content integrity verification — runs when `layout-integrity` is listed in the spec's `## Visual Focus` section. All four areas use the same dual pattern: `playwright-cli eval` for deterministic DOM/CSS probes (high confidence) + `playwright-cli screenshot` + visual judgment for perceptual quality (medium confidence).

**Security note:** When reading CSS custom properties via `playwright-cli eval`, do not include property names beginning with `--auth`, `--token`, `--key`, or `--secret` in reports.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stub note and add Performance & Responsive section header + PERF-01 Core Web Vitals</name>
  <files>agents/qa-tester.md</files>
  <read_first>
    - agents/qa-tester.md (lines 745-755 for stub note context)
    - agents/qa-tester.md (lines 1799-1805 for insertion point)
    - agents/qa-tester.md (lines 1421-1430 for section header pattern)
    - .planning/phases/12-performance-responsive/12-RESEARCH.md (Pattern 1 — Global Accumulator)
    - .planning/phases/12-performance-responsive/12-PATTERNS.md (Section Header and Security Note Pattern, Numbered Procedure Step Pattern, eval Snippet Format Pattern, run-code Multiline Block Pattern, Confidence Closing Line Pattern)
  </read_first>
  <action>
1. DELETE lines 747-748 from agents/qa-tester.md (the "**Note:** Tier 2 visual methodology for `performance-responsive`..." paragraph). Leave line 746 (the "This is a depth toggle" paragraph ending) and line 749 (### Design Verification) intact.

2. INSERT the following new section at the line that was previously 1803 (immediately before `### Design Reference`). After deletion of 2 lines in step 1, this becomes approximately line 1801. Insert AFTER the last row of the Layout & Content Integrity reporting table (`| Visual alignment/spacing judgment from screenshot | Medium |`) and BEFORE `### Design Reference`.

The new section content:

```markdown
### Performance & Responsive

Tier 2 performance and responsive verification — runs when `performance-responsive` is listed in the spec's `## Visual Focus` section. All four areas use the same dual pattern: `playwright-cli eval` for deterministic DOM/CSS probes (high confidence) + `playwright-cli screenshot` + visual judgment for perceptual quality (medium confidence).

**Security note:** When reading CSS custom properties via `playwright-cli eval`, do not include property names beginning with `--auth`, `--token`, `--key`, or `--secret` in reports.

**Lab-environment note:** All Web Vitals measurements in this section are **synthetic lab values** from the test environment — not field data or CrUX metrics. Always label findings with "Lab" prefix (e.g., "Lab LCP: 3.2s") and include this disclaimer in the performance section of every report: "Web Vitals measured in lab environment — values reflect test conditions, not real user experience."

#### Core Web Vitals (PERF-01)

Measure CLS, LCP, and INP using raw PerformanceObserver. No external libraries, no CDN fetches — browser-native APIs only (per D-01). Identify which elements or interactions cause poor scores via entry attributes.

**Thresholds (Google official):**

| Metric | Good | Needs Improvement | Poor |
| --- | --- | --- | --- |
| LCP | <= 2.5s | 2.5s - 4.0s | > 4.0s |
| INP | <= 200ms | 200ms - 500ms | > 500ms |
| CLS | <= 0.1 | 0.1 - 0.25 | > 0.25 |

**Browser support note:** `largest-contentful-paint` and `layout-shift` PerformanceObserver types are not supported in Firefox. PERF-01 CLS and LCP run in Chromium and WebKit only. INP is measurable in Firefox 144+ via `event` type. If an observer type throws on creation, note "[engine] [metric] not supported — skipped" and continue.

**Procedure:**

1. **Inject PerformanceObserver globals** (immediately after page load completes). Use `run-code` to set up three observers that write to `window.__qa*` globals:
   ```
   playwright-cli run-code "async (page) => {
     await page.evaluate(() => {
       window.__qaLCP = { value: 0, element: null, url: null };
       try {
         new PerformanceObserver((list) => {
           const entries = list.getEntries();
           const last = entries[entries.length - 1];
           window.__qaLCP = {
             value: last.startTime,
             element: last.element ? last.element.tagName + (last.element.id ? '#' + last.element.id : '') + (last.element.className ? '.' + String(last.element.className).split(' ')[0] : '') : null,
             url: last.url || null
           };
         }).observe({ type: 'largest-contentful-paint', buffered: true });
       } catch(e) { window.__qaLCP = { value: -1, element: null, url: null, error: 'not supported' }; }

       window.__qaCLS = { value: 0, largestSource: null };
       try {
         new PerformanceObserver((list) => {
           for (const entry of list.getEntries()) {
             if (!entry.hadRecentInput) {
               window.__qaCLS.value += entry.value;
               if (entry.sources && entry.sources.length > 0) {
                 const src = entry.sources[0];
                 if (src.node) {
                   window.__qaCLS.largestSource = src.node.tagName + (src.node.id ? '#' + src.node.id : '');
                 }
               }
             }
           }
         }).observe({ type: 'layout-shift', buffered: true });
       } catch(e) { window.__qaCLS = { value: -1, largestSource: null, error: 'not supported' }; }

       window.__qaINP = { value: 0, type: null, target: null };
       new PerformanceObserver((list) => {
         for (const entry of list.getEntries()) {
           if (entry.duration > window.__qaINP.value) {
             window.__qaINP = {
               value: entry.duration,
               type: entry.name,
               target: entry.target ? entry.target.tagName + (entry.target.id ? '#' + entry.target.id : '') : null
             };
           }
         }
       }).observe({ type: 'event', durationThreshold: 0, buffered: true });
     });
     return 'observers attached';
   }"
   ```

2. **Trigger LCP finalization** (per D-02). The browser does not emit the final `largest-contentful-paint` entry until user input occurs:
   ```
   playwright-cli click body
   ```
   If the page has interactive elements that were part of your functional testing, those clicks already satisfy this requirement. Only add an explicit body click if no other interaction occurred after navigation.

3. **Finalize CLS** (per D-03). Dispatch `visibilitychange` to flush the accumulated layout shift score:
   ```
   playwright-cli run-code "async (page) => {
     await page.evaluate(() => {
       document.dispatchEvent(new Event('visibilitychange'));
     });
     return 'visibilitychange dispatched';
   }"
   ```
   Do NOT use `page.close()` — that ends the session.

4. **Read all three metrics:**
   ```
   playwright-cli eval "() => ({ lcp: window.__qaLCP, cls: window.__qaCLS, inp: window.__qaINP })"
   ```

5. **Evaluate results and attribute findings:**
   - If `lcp.value === -1` or `cls.value === -1`: note "[engine] [metric] not supported — skipped"
   - If `lcp.value > 2500`: flag as finding — "Lab LCP: [value/1000]s ([lcp.element]) — [Good/Needs Improvement/Poor]"
   - If `cls.value > 0.1`: flag as finding — "Lab CLS: [cls.value] (largest shift source: [cls.largestSource]) — [Good/Needs Improvement/Poor]"
   - If `inp.value === 0` AND no agent interactions occurred on this page: report "Lab INP: not measured (no interactions recorded)" — do NOT report as "Good" or score 0 (per D-04)
   - If `inp.value > 200`: flag as finding — "Lab INP: [inp.value]ms (slowest interaction: [inp.type] on [inp.target]) — [Good/Needs Improvement/Poor]"
   - For each finding, include the threshold category (Good/Needs Improvement/Poor) and the attributed element

6. **Screenshot supplement:** Take a screenshot of the page. Use visual judgment to identify potential causes of poor LCP (large unoptimized hero images, late-loading above-fold content) or CLS (ads, lazy-loaded images without explicit dimensions, dynamically injected banners). Note visual observations at Medium confidence.

Confidence: Eval-confirmed poor LCP (value > 2500ms from PerformanceObserver) = High. Eval-confirmed poor CLS (value > 0.1 from PerformanceObserver) = High. Eval-confirmed poor INP (value > 200ms from event observer) = High. INP not measured (no interactions) = Observation (not a finding). Visual judgment of likely performance cause from screenshot = Medium.
```

3. EXTEND the Confidence Summary Table (located at approximately line 372, after `| Focus not moving to first error after submission | Medium |`). Append these rows to the existing table:

```markdown
| Lab LCP > 2.5s (PerformanceObserver entry) | High |
| Lab CLS > 0.1 (PerformanceObserver accumulated) | High |
| Lab INP > 200ms (event observer entry) | High |
| Cross-browser CSS property divergence (getComputedStyle diff) | High |
| Cross-browser visual difference (screenshot comparison) | Medium |
| Visual judgment of likely performance cause | Medium |
```
  </action>
  <verify>
    <automated>grep -c "#### Core Web Vitals (PERF-01)" agents/qa-tester.md && grep -c "window.__qaLCP" agents/qa-tester.md && grep -c "window.__qaCLS" agents/qa-tester.md && grep -c "window.__qaINP" agents/qa-tester.md && grep -c "Lab LCP" agents/qa-tester.md && grep -c "visibilitychange" agents/qa-tester.md && ! grep -q "Tier 2 visual methodology for .performance-responsive. will be added in Phase 12" agents/qa-tester.md && echo "STUB_REMOVED_OK"</automated>
  </verify>
  <acceptance_criteria>
    - agents/qa-tester.md contains `### Performance & Responsive` (1 occurrence)
    - agents/qa-tester.md contains `#### Core Web Vitals (PERF-01)` (1 occurrence)
    - agents/qa-tester.md contains `window.__qaLCP` (at least 3 occurrences — injection, read, evaluation)
    - agents/qa-tester.md contains `window.__qaCLS` (at least 3 occurrences)
    - agents/qa-tester.md contains `window.__qaINP` (at least 3 occurrences)
    - agents/qa-tester.md contains `Lab LCP` (at least 2 occurrences — threshold description + finding format)
    - agents/qa-tester.md contains `visibilitychange` (at least 1 occurrence in the new section)
    - agents/qa-tester.md does NOT contain "Tier 2 visual methodology for `performance-responsive` will be added in Phase 12" (stub note removed)
    - agents/qa-tester.md contains `**Lab-environment note:**` (1 occurrence)
    - agents/qa-tester.md contains `Lab LCP > 2.5s (PerformanceObserver entry) | High` in the confidence table
    - `### Performance & Responsive` appears AFTER `| Visual alignment/spacing judgment from screenshot | Medium |` and BEFORE `### Design Reference`
    - `**Security note:**` appears in the Performance & Responsive section
  </acceptance_criteria>
  <done>PERF-01 Core Web Vitals methodology complete with PerformanceObserver injection, LCP/CLS finalization triggers, metric reading, threshold evaluation, element attribution, lab-value labeling, Firefox caveats, and try/catch safety. Stub note removed. Confidence table extended with Web Vitals entries.</done>
</task>

<task type="auto">
  <name>Task 2: Add PERF-02 Cross-Browser Rendering Comparison subsection</name>
  <files>agents/qa-tester.md</files>
  <read_first>
    - agents/qa-tester.md (read the newly added PERF-01 section to find insertion point — immediately after PERF-01's Confidence closing line)
    - .planning/phases/12-performance-responsive/12-RESEARCH.md (Pattern 2 — getComputedStyle Property Diff, Cross-Browser Property List section, Pitfall 3)
    - .planning/phases/12-performance-responsive/12-PATTERNS.md (getComputedStyle multi-property read pattern, Engine-tagged findings pattern)
  </read_first>
  <action>
INSERT the following subsection immediately after PERF-01's closing Confidence line (which ends with "Visual judgment of likely performance cause from screenshot = Medium."), and BEFORE the line where PERF-03 will later be added (for now, before `### Design Reference`):

```markdown
#### Cross-Browser Rendering Comparison (PERF-02)

Compare rendered CSS across browser engines. Use `getComputedStyle` and `getBoundingClientRect` eval probes as the primary detection mechanism (per D-07). Chromium is the baseline — any other engine that diverges gets flagged as a cross-browser finding (per D-08).

**When this runs:** Only when the agent is executing a multi-engine run (multiple engines listed in spec's `## Browsers` section). During a single-engine run, skip PERF-02 entirely and note "Cross-browser comparison requires multi-engine run — skipped (single engine active)."

**Target properties (~18 for comparison):**

| Property | Category | Notes |
| --- | --- | --- |
| `fontFamily` | Typography | Exempt if only generic family name differs |
| `fontWeight` | Typography | 700 vs "bold" keyword normalization |
| `lineHeight` | Typography | Sub-pixel rounding differences |
| `letterSpacing` | Typography | WebKit spacing variance |
| `gap` | Layout | Flex/grid gap |
| `borderRadius` | Rendering | Sub-pixel rendering |
| `position` | Layout | Check sticky threshold |
| `display` | Layout | Flex/grid support |
| `overflowX` | Layout | Value resolution differences |
| `overflowY` | Layout | Value resolution differences |
| `transform` | Rendering | 3D transform differences |
| `opacity` | Rendering | Sub-pixel compositing |
| `boxShadow` | Rendering | Color format (rgb vs hex) |
| `backdropFilter` | Rendering | WebKit requires -webkit- prefix |
| `textRendering` | Typography | geometricPrecision vs auto |
| `alignItems` | Layout | "normal" vs "stretch" resolution |
| `justifyContent` | Layout | "normal" vs "flex-start" resolution |
| `fontSmoothing` | Typography | -webkit-font-smoothing (WebKit only) |

**Intentional divergences (EXEMPT — do not flag):** per D-10:
- Native form controls: `<select>`, `<input type="date">`, `<input type="range">`
- Scrollbar appearance and `scrollbar-width`
- System font fallback names (e.g., `-apple-system` vs `BlinkMacSystemFont`)
- `-webkit-` prefixed vendor properties present only in WebKit

**Procedure:**

1. **Select target elements for comparison.** Query for key structural elements — use the same element set you'd evaluate for DESIGN-05 cross-page consistency:
   ```
   playwright-cli eval "() => {
     const selectors = ['body', 'header', 'nav', 'main', '[role=\"main\"]', 'h1', 'h2', 'p', 'button', '[role=\"button\"]', 'a', 'footer', '.container', '.wrapper'];
     const results = {};
     for (const sel of selectors) {
       const el = document.querySelector(sel);
       if (!el) continue;
       const s = getComputedStyle(el);
       results[sel] = {
         fontFamily: s.fontFamily,
         fontWeight: s.fontWeight,
         lineHeight: s.lineHeight,
         letterSpacing: s.letterSpacing,
         gap: s.gap,
         borderRadius: s.borderRadius,
         position: s.position,
         display: s.display,
         overflowX: s.overflowX,
         overflowY: s.overflowY,
         transform: s.transform,
         opacity: s.opacity,
         boxShadow: s.boxShadow,
         backdropFilter: s.backdropFilter,
         textRendering: s.textRendering,
         alignItems: s.alignItems,
         justifyContent: s.justifyContent
       };
     }
     return results;
   }"
   ```

2. **Compare against Chromium baseline.** The Chromium run's eval results serve as the reference. For each element + property pair, compare the current engine's value against the Chromium value:
   - **Exact match:** PASS (no finding)
   - **Format-only difference** (e.g., `rgb(0, 0, 0)` vs `#000000`, `700` vs `bold`): normalize before comparing — these are not real divergences
   - **Value difference** (e.g., different computed lineHeight, different gap value): check if the element is an exempt form control or the property is in the exemption list. If exempt, skip. Otherwise, flag as finding.

3. **Format cross-browser findings using engine-tagged pattern:**
   ```
   [webkit] [selector] — [property]: "[webkit value]" vs chromium "[chromium value]"
   ```
   Example: `[webkit] h1 — lineHeight: "1.15" vs chromium "1.2"`

4. **Screenshot supplement** (per D-11). After eval comparison, take screenshots at the same viewport size in the current engine. Use visual judgment to identify perceptual differences that individual property probes miss — things like sub-pixel text rendering, shadow intensity, gradient banding. Note visual-only observations at Medium confidence:
   ```
   playwright-cli screenshot
   ```
   Compare visually against the Chromium screenshot (from the prior engine run). Flag noticeable rendering differences not caught by the property diff.

5. **No-divergence shortcut:** If all ~18 properties match across all target elements AND the screenshot shows no visual difference, report: "[engine] rendering comparison — no divergences detected."

Confidence: Eval-confirmed CSS property divergence (getComputedStyle diff, non-exempt property, non-format difference) = High. Visual rendering difference from screenshot comparison (not caught by eval) = Medium.
```
  </action>
  <verify>
    <automated>grep -c "#### Cross-Browser Rendering Comparison (PERF-02)" agents/qa-tester.md && grep -c "getComputedStyle" agents/qa-tester.md && grep -c "Chromium baseline" agents/qa-tester.md && grep -c "Intentional divergences" agents/qa-tester.md && grep -c "Cross-browser CSS property divergence" agents/qa-tester.md</automated>
  </verify>
  <acceptance_criteria>
    - agents/qa-tester.md contains `#### Cross-Browser Rendering Comparison (PERF-02)` (1 occurrence)
    - agents/qa-tester.md contains `getComputedStyle` (at least 2 occurrences in new section — procedure step + property table context)
    - agents/qa-tester.md contains `Chromium baseline` or `Chromium is the baseline` (at least 1 occurrence)
    - agents/qa-tester.md contains `Intentional divergences (EXEMPT` (1 occurrence)
    - agents/qa-tester.md contains `backdropFilter` in the target property table
    - agents/qa-tester.md contains the engine-tagged finding format `[webkit] [selector]`
    - agents/qa-tester.md contains `fontFamily` in the eval snippet
    - PERF-02 subsection appears AFTER PERF-01's confidence line and BEFORE `### Design Reference`
    - agents/qa-tester.md contains `Cross-browser CSS property divergence (getComputedStyle diff) | High` in the confidence summary table (added in Task 1)
  </acceptance_criteria>
  <done>PERF-02 Cross-Browser Rendering Comparison methodology complete with 18-property target list, eval probe procedure, Chromium baseline comparison, intentional divergence exemption list, engine-tagged finding format, and screenshot supplement. The full PERF-01 + PERF-02 methodology is now in place between Layout & Content Integrity and Design Reference sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> Page (eval) | Agent injects JS into page context via playwright-cli eval/run-code |
| Page -> Agent (results) | Page returns eval results that agent interprets and reports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | PERF-02 getComputedStyle eval | mitigate | Security note included: "do not include property names beginning with `--auth`, `--token`, `--key`, or `--secret` in reports" — same mitigation as Phases 9-11 |
| T-12-02 | Information Disclosure | PERF-01 PerformanceObserver element attribution | accept | Element tagName + id + className is non-sensitive structural info; no textContent or user data exposed |
| T-12-03 | Denial of Service | PERF-01 observer accumulation (window globals) | accept | Globals use fixed-size objects (single LCP/CLS/INP entry); no unbounded accumulation possible |
| T-12-04 | Tampering | Page JS clearing window.__qa* globals | accept | Low probability (non-conflicting namespace); if cleared, agent gets stale/zero values and reports "not measured" — no incorrect assertion |
</threat_model>

<verification>
- `grep -c "### Performance & Responsive" agents/qa-tester.md` returns 1
- `grep -c "#### Core Web Vitals (PERF-01)" agents/qa-tester.md` returns 1
- `grep -c "#### Cross-Browser Rendering Comparison (PERF-02)" agents/qa-tester.md` returns 1
- `grep "Tier 2 visual methodology for" agents/qa-tester.md` returns NO results (stub note removed)
- `grep -c "Lab LCP" agents/qa-tester.md` returns at least 2
- `grep -c "Lab CLS" agents/qa-tester.md` returns at least 2
- `grep -c "Lab INP" agents/qa-tester.md` returns at least 2
- Ordering check: `grep -n "### Performance\|### Design Reference" agents/qa-tester.md` shows Performance & Responsive appears BEFORE Design Reference
</verification>

<success_criteria>
- Performance & Responsive section header exists with dual-pattern intro, security note, and lab-environment note
- PERF-01 implements full PerformanceObserver lifecycle: inject observers -> interact (LCP finalization) -> visibilitychange (CLS finalization) -> read globals -> evaluate vs thresholds -> attribute elements
- PERF-02 implements getComputedStyle comparison across 18 properties with Chromium baseline, exemption list, engine-tagged finding format, and screenshot supplement
- Stub note "Tier 2 visual methodology for `performance-responsive` will be added in Phase 12" is deleted
- Confidence Summary Table extended with 6 new PERF rows
- All findings explicitly labeled as lab-environment values
</success_criteria>

<output>
After completion, create `.planning/phases/12-performance-responsive/12-01-SUMMARY.md`
</output>
