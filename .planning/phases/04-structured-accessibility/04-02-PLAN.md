---
phase: 04-structured-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - agents/qa-tester.md
  - skills/qa-run.md
autonomous: true

must_haves:
  truths:
    - "Agent measures touch target sizes and flags elements below 44x44px at mobile viewport"
    - "Agent checks that color is not the sole indicator for state changes"
    - "Agent checks prefers-reduced-motion is respected"
    - "Agent verifies zoom to 200% works without horizontal scroll or content loss"
    - "Agent verifies error summary exists after form submission with errors"
    - "Agent verifies error summary links to individual error fields"
    - "Agent checks aria-invalid is set on errored fields"
    - "Agent verifies focus moves to first error after form submission"
  artifacts:
    - path: "agents/qa-tester.md"
      provides: "Interactive Elements and Form Accessibility methodology sections"
      contains: "Touch Target"
    - path: "skills/qa-run.md"
      provides: "Interactive element and form accessibility instructions in agent prompt"
      contains: "touch target"
  key_links:
    - from: "skills/qa-run.md"
      to: "agents/qa-tester.md"
      via: "Agent prompt references interactive element and form accessibility methodology"
      pattern: "touch target|reduced motion|zoom|aria-invalid|error summary"
---

<objective>
Add Interactive Elements and Form Accessibility methodology to the QA agent — covering touch target sizes, color as sole indicator, reduced motion, zoom behavior, and form error patterns (error summary, field links, aria-invalid, focus on first error).

Purpose: A11Y-10 through A11Y-17 — this completes the Tier 2 accessibility methodology by adding interactive element checks and structured form accessibility patterns that go beyond the existing form intelligence from Phase 3.

Output: Updated agents/qa-tester.md with Interactive Elements and Form Accessibility subsections added to the Structured Accessibility section (created in Plan 01), updated skills/qa-run.md with additional accessibility instructions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-structured-accessibility/04-RESEARCH.md
@.planning/phases/04-structured-accessibility/04-01-SUMMARY.md
@agents/qa-tester.md
@skills/qa-run.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Interactive Elements and Form Accessibility methodology to qa-tester agent</name>
  <files>agents/qa-tester.md</files>
  <action>
Add two new subsections to the "## Structured Accessibility" section in agents/qa-tester.md (created by Plan 01), placed after the Page Structure subsection.

**### Interactive Elements**

**Touch Target Sizes (A11Y-10):**
At mobile viewport (375x812) specifically — NOT at desktop:
1. Query all interactive elements: `a, button, [role="button"], input, select, textarea, [tabindex]`
2. For each visible element, get its bounding box via `locator.boundingBox()`
3. Flag any element where width < 44px OR height < 44px
4. Report the element's accessible name (aria-label, text content, or tag) and its actual dimensions
5. `boundingBox()` returns null for hidden elements — skip those (they're not visible at this viewport)
6. This check aligns with Phase 3's multi-viewport testing — run it as part of the mobile viewport pass

Confidence: Elements below 44x44px at mobile = High confidence.

**Color as Sole Indicator (A11Y-11):**
When testing state changes (hover, active, selected, error, disabled):
- Take screenshots of the element in both states
- Check whether the state change is communicated by MORE than just color — look for: text labels, icons, underlines, borders, opacity changes, shape changes
- This is a visual judgment call — Playwright cannot compute color differences programmatically without axe-core
- Frame findings as Medium confidence observations
- Common patterns to flag: links distinguished from text only by color (no underline), error states shown only by red text (no icon or border), selected tabs with only a color change

Confidence: Color-only state indicator = Medium confidence (requires visual judgment, cannot be mechanically verified).

**Reduced Motion (A11Y-12):**
On pages with animations, carousels, or loading spinners:
1. Use `page.emulateMedia({ reducedMotion: 'reduce' })` to set the preference
2. Take a screenshot to visually confirm animations are suppressed
3. Optionally check computed styles on animated elements: `animation-duration` and `transition-duration` should be `0s` or very short
4. Important: Check the VISUALLY ANIMATED element specifically, not its parent container — CSS animations can be on any element in the subtree
5. After checking, reset with `page.emulateMedia({ reducedMotion: 'no-preference' })` or `null`
6. If page has no visible animations, skip this check (not applicable)

Confidence: Animations still running with reduced motion = High confidence. Property check on wrong element (parent vs child) may give false results — if `animation-duration` returns `0s` but animations are visually present, note as needs investigation.

**Zoom to 200% (A11Y-13):**
At desktop viewport (1280px) — NOT mobile (mobile already represents a zoomed-down view):
1. Simulate text zoom: `page.evaluate(() => { document.documentElement.style.fontSize = '200%' })`
2. Check for horizontal overflow: `document.documentElement.scrollWidth > document.documentElement.clientWidth` — should be false
3. Check that no content is clipped or lost — take a screenshot
4. Additionally, test responsive proxy: temporarily resize viewport to half width (640px) and check that content reflows without horizontal scrolling
5. After checking, restore: `page.evaluate(() => { document.documentElement.style.fontSize = '' })` and restore viewport

Important limitation: This is a PROXY for real browser zoom, not identical. The CSS font-size approach tests text zoom (WCAG 1.4.4). The viewport-halving tests responsive layout. Both are reasonable for automated testing. If both pass, note: "Zoom proxy tests passed — manual verification at 200% browser zoom recommended for final validation."

Confidence: Horizontal overflow or content loss at simulated 200% = High confidence.

**### Form Accessibility**

Note: These checks complement the existing Form Intelligence section (from Phase 3). Form Intelligence covers functional form testing (validation, submission states, autofill). This section covers the accessibility dimension of form error handling.

**Error Summary (A11Y-14):**
After submitting a form with validation errors:
- Check that an error summary appears (a container listing all errors, typically at the top of the form or as an alert)
- The error summary should be an element with `role="alert"` or inside an `aria-live` region
- If no error summary exists and errors are only shown inline, flag as Medium confidence

**Error Summary Links (A11Y-15):**
If an error summary exists:
- Each error message in the summary should be a link that navigates to (focuses on) the corresponding errored field
- Click each error link and verify focus moves to the correct input field
- If error messages are plain text (not links), flag as Medium confidence

**aria-invalid on Errored Fields (A11Y-16):**
After form submission with errors:
- For each field that has a validation error, check that `aria-invalid="true"` is set
- Use `element.getAttribute('aria-invalid')` via evaluate
- Missing `aria-invalid` on a visibly errored field = Medium confidence

**Focus on First Error (A11Y-17):**
After form submission with errors:
- Verify that focus moved to either: (a) the error summary container, or (b) the first errored field
- Use `document.activeElement` check — it should be inside the error summary or on the first invalid input
- If focus stays on the submit button or elsewhere, flag as Medium confidence

**Reporting table (add to the existing confidence table in Structured Accessibility):**

| Finding type | Confidence |
|---|---|
| Touch target below 44x44px at mobile | High |
| Animations running with prefers-reduced-motion: reduce | High |
| Horizontal overflow at 200% zoom proxy | High |
| Color as sole state indicator | Medium |
| No form error summary | Medium |
| Error summary without links to fields | Medium |
| Missing aria-invalid on errored field | Medium |
| Focus not moving to first error after submission | Medium |
  </action>
  <verify>
grep -c "Touch Target" agents/qa-tester.md should return at least 1.
grep -c "Color as Sole Indicator" agents/qa-tester.md should return at least 1 (case insensitive).
grep -c "Reduced Motion" agents/qa-tester.md should return at least 1.
grep -c "Zoom" agents/qa-tester.md should return at least 1.
grep -c "Error Summary" agents/qa-tester.md should return at least 1.
grep -c "aria-invalid" agents/qa-tester.md should return at least 1.
grep -c "Form Accessibility" agents/qa-tester.md should return at least 1.
  </verify>
  <done>agents/qa-tester.md Structured Accessibility section now includes Interactive Elements (touch targets, color sole indicator, reduced motion, zoom) and Form Accessibility (error summary, field links, aria-invalid, focus on first error) subsections with confidence levels.</done>
</task>

<task type="auto">
  <name>Task 2: Add interactive element and form accessibility instructions to qa-run agent prompt</name>
  <files>skills/qa-run.md</files>
  <action>
Update skills/qa-run.md to add interactive element and form accessibility instructions to the agent prompt template (the "## Instructions" block within the agent invocation).

Add to the existing accessibility instructions bullet list (added by Plan 01), expanding it:

```
  - Touch targets: At mobile viewport, measure all interactive elements — flag any below 44x44px
  - Color sole indicator: When testing state changes, verify non-color indicators exist (text, icon, border)
  - Reduced motion: Use emulateMedia to set prefers-reduced-motion: reduce, verify animations stop
  - Zoom: At desktop viewport, simulate 200% text zoom and check for overflow or content loss
  - Form errors: After submission with errors, check for error summary, links to fields, aria-invalid on errored fields, and focus on first error
```

Also update the "## Output" section accessibility line (added by Plan 01) to be more comprehensive. Change:

```
  - Accessibility findings (focus management, page structure, skip links, landmarks)
```

to:

```
  - Accessibility findings (focus management, page structure, skip links, landmarks, touch targets, reduced motion, zoom, form error patterns)
```
  </action>
  <verify>
grep -c "touch target" skills/qa-run.md should return at least 1 (case insensitive).
grep -c "reduced motion" skills/qa-run.md should return at least 1 (case insensitive).
grep -c "aria-invalid" skills/qa-run.md should return at least 1.
grep -c "zoom" skills/qa-run.md should return at least 1 (case insensitive).
grep "form error patterns" skills/qa-run.md should return a match.
  </verify>
  <done>qa-run skill instructs the agent to perform interactive element checks (touch targets, color, reduced motion, zoom) and form accessibility checks (error summary, field links, aria-invalid, focus) and includes all accessibility categories in the output summary.</done>
</task>

</tasks>

<verification>
- agents/qa-tester.md Structured Accessibility section now has all four subsections: Focus Management, Page Structure, Interactive Elements, Form Accessibility
- A11Y-10: Touch targets — covered by measurement at mobile viewport with 44x44px threshold
- A11Y-11: Color sole indicator — covered by visual judgment methodology with Medium confidence
- A11Y-12: Reduced motion — covered by emulateMedia approach targeting animated elements
- A11Y-13: Zoom — covered by CSS font-size 200% + viewport halving proxy with limitation noted
- A11Y-14: Error summary — covered by checking for summary container with role="alert" or aria-live
- A11Y-15: Error summary links — covered by clicking error links and verifying focus moves to field
- A11Y-16: aria-invalid — covered by checking attribute on visibly errored fields
- A11Y-17: Focus on first error — covered by checking document.activeElement after submission
- skills/qa-run.md agent prompt includes all interactive element and form accessibility instructions
- Confidence levels align with research recommendations
</verification>

<success_criteria>
A11Y-10 through A11Y-17 fully addressed in agent methodology and skill prompt. Interactive elements cover touch targets (mobile viewport, 44x44px), color sole indicator (visual judgment), reduced motion (emulateMedia), and zoom (CSS proxy + viewport halving). Form accessibility covers error summary, field links, aria-invalid, and focus on first error. All findings use the established confidence framework.
</success_criteria>

<output>
After completion, create `.planning/phases/04-structured-accessibility/04-02-SUMMARY.md`
</output>
