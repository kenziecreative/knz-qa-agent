---
phase: 01-cleanup-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/qa-tester.md
  - skills/qa-run.md
  - skills/qa-check.md
  - skills/qa-gen.md
  - skills/qa-init.md
  - skills/qa-report.md
  - .claude-plugin/plugin.json
  - .claude-plugin/.mcp.json
  - commands/ (delete directory)
  - SPEC-advanced-accessibility-agent.md (delete)
  - SPEC-cli-testing-support.md (delete)
autonomous: true

must_haves:
  truths:
    - "No file in the repo references `mcp__MCP_DOCKER__browser_*` (except .planning docs)"
    - "No file in the repo lists `Task` as a tool (except .planning docs)"
    - "qa-tester agent lists exactly: mcp__playwright__*, Read, Write, Bash, Glob, Grep"
    - "Plugin bundles Playwright MCP via .mcp.json in headless mode"
    - "plugin.json has version and settings defaults"
    - "commands/ directory no longer exists"
    - "Root-level SPEC files no longer exist"
  artifacts:
    - path: "agents/qa-tester.md"
      provides: "Clean-slate agent with correct tool list and refined methodology"
      contains: "mcp__playwright__"
    - path: ".claude-plugin/.mcp.json"
      provides: "Playwright MCP server configuration"
      contains: "playwright"
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest with version and settings"
      contains: "settings"
  key_links:
    - from: "agents/qa-tester.md"
      to: ".claude-plugin/.mcp.json"
      via: "Agent references mcp__playwright__* tools which .mcp.json provides"
      pattern: "mcp__playwright__"
    - from: "skills/qa-run.md"
      to: "agents/qa-tester.md"
      via: "Skill spawns qa-tester agent"
      pattern: "qa-tester"
---

<objective>
Clean up the plugin structure: delete stale commands and SPEC files, rewrite the qa-tester agent with correct tools, update all skill frontmatter, bundle Playwright MCP, and update the plugin manifest.

Purpose: Establish a clean, modern plugin foundation with no stale references and correct tool access.
Output: Restructured plugin with agent, skills, MCP config, and manifest all consistent and current.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cleanup-foundation/01-CONTEXT.md

@agents/qa-tester.md
@.claude-plugin/plugin.json
@skills/qa-run.md
@skills/qa-check.md
@skills/qa-gen.md
@skills/qa-init.md
@skills/qa-report.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete stale files and rewrite agent</name>
  <files>
    agents/qa-tester.md
    commands/ (delete entire directory)
    SPEC-advanced-accessibility-agent.md (delete)
    SPEC-cli-testing-support.md (delete)
  </files>
  <action>
    1. Delete the entire `commands/` directory (5 files: run.md, check.md, gen.md, init.md, report.md). Skills fully replace them.

    2. Delete `SPEC-advanced-accessibility-agent.md` (accessibility agent is a separate project) and `SPEC-cli-testing-support.md` (CLI testing is out of scope for v0.2).

    3. Rewrite `agents/qa-tester.md` as a clean-slate rewrite with these changes:
       - Tools list MUST be exactly: `mcp__playwright__*`, `Read`, `Write`, `Bash`, `Glob`, `Grep`
       - Remove `mcp__MCP_DOCKER__browser_*` (stale Docker-based reference)
       - Remove `WebSearch` (not needed for QA testing)
       - Add `Write` (needed for writing reports)
       - Add `Bash` (needed for directory creation, commands)
       - Add `Grep` (needed for finding files)
       - Keep the core methodology (test like a human, pattern recognition, escalation, spec skepticism, confidence levels, accessibility baseline)
       - Refine at Claude's discretion — the methodology is solid, but tool references within the body should be updated if any mention Docker/MCP_DOCKER tools
  </action>
  <verify>
    Run these checks:
    - `ls commands/` should fail (directory gone)
    - `ls SPEC-advanced-accessibility-agent.md SPEC-cli-testing-support.md` should fail (files gone)
    - `grep -r "mcp__MCP_DOCKER" agents/` should return nothing
    - `grep -r "WebSearch" agents/` should return nothing
    - `grep "mcp__playwright__" agents/qa-tester.md` should return a match
    - `grep "Write" agents/qa-tester.md` and `grep "Bash" agents/qa-tester.md` should return matches (in tools list)
  </verify>
  <done>
    commands/ directory deleted, root SPEC files deleted, qa-tester.md rewritten with correct tool list (mcp__playwright__*, Read, Write, Bash, Glob, Grep) and no stale references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update skills frontmatter, create .mcp.json, update plugin.json</name>
  <files>
    skills/qa-run.md
    skills/qa-check.md
    skills/qa-gen.md
    skills/qa-init.md
    skills/qa-report.md
    .claude-plugin/.mcp.json
    .claude-plugin/plugin.json
  </files>
  <action>
    1. Update frontmatter `allowed-tools` in ALL skill files. The current `commands/run.md` (now deleted) had stale tool refs — but the skills themselves also need review:
       - `skills/qa-run.md` currently lists: Read, Write, Bash, Grep, Glob, Task, mcp__MCP_DOCKER__browser_*
         Change to: Read, Write, Bash, Grep, Glob, Agent, mcp__playwright__*
         (Task -> Agent, mcp__MCP_DOCKER__browser_* -> mcp__playwright__*)
       - For all other skills (`qa-check.md`, `qa-gen.md`, `qa-init.md`, `qa-report.md`): check their frontmatter. If they list `Task` or `mcp__MCP_DOCKER__browser_*`, update to `Agent` and `mcp__playwright__*` respectively. If they don't have `allowed-tools` frontmatter, they use the skill's default tools — leave them alone.
       - Do NOT change body content that uses the word "task" in English prose (e.g., "## Verification Task" heading, "ad-hoc task" description). Only change tool references.

    2. Create `.claude-plugin/.mcp.json` with Playwright MCP server configuration:
       ```json
       {
         "mcpServers": {
           "playwright": {
             "command": "npx",
             "args": ["@playwright/mcp@latest", "--headless"]
           }
         }
       }
       ```
       Use npx (downloads on demand, no local install). Headless mode by default (more stable, no window interference).

    3. Update `.claude-plugin/plugin.json`:
       - Bump version (Claude's discretion: 0.2.0-alpha or 0.2.0-dev recommended since this is start of v0.2 work)
       - Add settings defaults (Claude's discretion on what to expose — consider things the agent actually uses like default viewport, report format, headless mode toggle)
       - Keep existing name, description, author fields
  </action>
  <verify>
    Run these checks:
    - `grep "Task" skills/qa-run.md` should NOT match in the frontmatter tools list (may match in body prose which is fine)
    - `grep "mcp__MCP_DOCKER" skills/*.md` should return nothing
    - `cat .claude-plugin/.mcp.json` should show valid JSON with playwright config
    - `cat .claude-plugin/plugin.json` should show updated version and settings
    - `grep "mcp__playwright__" skills/qa-run.md` should return a match in frontmatter
  </verify>
  <done>
    All 5 skill files have correct tool references (no Task tool, no mcp__MCP_DOCKER). .mcp.json exists with Playwright MCP config (npx, headless). plugin.json has updated version and settings defaults.
  </done>
</task>

</tasks>

<verification>
Full stale reference scan across the entire repo (excluding .planning/ which documents the changes):

```bash
# Should return ZERO matches outside .planning/
grep -r "mcp__MCP_DOCKER" --include="*.md" . | grep -v ".planning/"
grep -r "  - Task$" --include="*.md" . | grep -v ".planning/"

# Positive checks
grep "mcp__playwright__" agents/qa-tester.md
cat .claude-plugin/.mcp.json
cat .claude-plugin/plugin.json
ls commands/ 2>&1  # Should fail
```
</verification>

<success_criteria>
1. commands/ directory does not exist
2. Root SPEC-*.md files do not exist
3. Zero references to mcp__MCP_DOCKER__browser_* outside .planning/
4. Zero `Task` tool references outside .planning/
5. qa-tester.md tools list is exactly: mcp__playwright__*, Read, Write, Bash, Glob, Grep
6. .claude-plugin/.mcp.json exists with Playwright MCP (npx, headless)
7. plugin.json has updated version and settings defaults
8. All 5 skill files have correct allowed-tools (where applicable)
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup-foundation/01-01-SUMMARY.md`
</output>
