---
phase: 07-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "README Skills section documents all 6 skills including /qa:monitor"
    - "README /qa:report examples show correct flags (--project, --since, --create-issues)"
    - "README Tier 2 accessibility area descriptions reference canonical names from SPEC-FORMAT.md"
    - "README Project Structure tree includes qa-monitor.md and hooks/ directory"
  artifacts:
    - path: "README.md"
      provides: "Accurate documentation matching actual implementation"
      contains: "/qa:monitor"
  key_links:
    - from: "README.md"
      to: "skills/qa-monitor.md"
      via: "skill documentation section"
      pattern: "/qa:monitor"
    - from: "README.md"
      to: "skills/qa-report.md"
      via: "skill examples"
      pattern: "--project.*--create-issues"
---

<objective>
Fix README documentation to accurately reflect the current implementation.

Purpose: README has drifted from the actual codebase — /qa:monitor skill is undocumented, /qa:report examples show flags that don't exist (--save) and omit flags that do (--project, --since, --create-issues), and the Tier 2 accessibility area descriptions don't use the canonical names.

Output: Updated README.md with all 6 skills documented, correct examples, and consistent terminology.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@skills/qa-monitor.md
@skills/qa-report.md
@examples/SPEC-FORMAT.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /qa:monitor skill documentation to README</name>
  <files>README.md</files>
  <action>
Add a new skill section for `/qa:monitor` between `/qa:gen` and `/qa:report` in the Skills section. Base content on skills/qa-monitor.md:

```markdown
### `/qa:monitor` - Continuous Monitoring

Run recurring smoke tests against a live URL, detect regressions, and persist results across sessions.

```bash
# Monitor with default settings (smoke tag, 5-min interval)
/qa:monitor --project ~/Projects/my-app --url https://my-app.example.com

# Monitor critical paths every 10 minutes, run 12 times (2 hours)
/qa:monitor --project ~/Projects/my-app --url https://staging.my-app.com --tag critical --interval 10 --runs 12
```

**Regressions:** Compares each run against a baseline of last-known-good results. When a previously passing scenario fails, it's flagged as a regression and an alert marker is written for the next session.
```

Also update the Project Structure tree to include:
- `qa-monitor.md` in the skills/ listing
- `hooks/` directory with `hooks.json`, `stop.md`, `session-start.md`

Also update the Version History 0.2.0 bullet list to mention "Six skills" instead of "Five skills" if applicable, and add continuous monitoring bullet.

Also update the Development Notes section: change "Current milestone: v0.2 - Robust Web/UI Testing (6 phases)" to "(7 phases)" since Phase 7 exists.
  </action>
  <verify>grep -c "qa:monitor" README.md should return at least 3 (section header, examples, project structure)</verify>
  <done>/qa:monitor has its own Skills subsection with usage examples, and Project Structure tree reflects all actual files</done>
</task>

<task type="auto">
  <name>Task 2: Fix /qa:report examples and Tier 2 a11y area names in README</name>
  <files>README.md</files>
  <action>
**Fix /qa:report skill section (lines ~96-101):**

Replace the current examples:
```
/qa:report
/qa:report --format json
/qa:report --save ./qa-report.md
```

With accurate examples matching skills/qa-report.md:
```bash
# Report on a project
/qa:report --project ~/Projects/my-app

# Report with trend analysis from last 14 days
/qa:report --project ~/Projects/my-app --since 14d

# Create GitHub issues from critical findings
/qa:report --project ~/Projects/my-app --create-issues

# JSON output
/qa:report --project ~/Projects/my-app --format json
```

Also update the /qa:report description line to mention cross-session reporting and trend analysis (matching the actual skill purpose).

**Fix Tier 2 accessibility area descriptions (lines ~318-321):**

Replace the informal bullet list:
```
- Focus management in modals (move in, trap, return)
- Skip links, landmarks, heading hierarchy
- Touch target sizing, zoom behavior, reduced motion
- Form error patterns (summary, field links, aria-invalid)
```

With bullets that reference the canonical area names from SPEC-FORMAT.md:
```
- **focus-management** — modal focus trapping, skip links, aria-live regions
- **page-structure** — heading hierarchy, landmarks, page title, lang attribute
- **interactive-elements** — touch targets, color sole indicator, reduced motion, zoom
- **form-accessibility** — error summary, error links, aria-invalid, focus on first error
```

This matches the exact naming from SPEC-FORMAT.md "Available focus areas" (lines 195-198) and the agent's Accessibility Focus Trigger section.
  </action>
  <verify>
Run: grep "qa:report --project" README.md (should match); grep "qa:report.*--save" README.md (should NOT match); grep "focus-management" README.md (should appear in Tier 2 section AND in Accessibility Focus section)
  </verify>
  <done>/qa:report examples use --project, --since, --create-issues (no --save). Tier 2 area bullets use canonical hyphenated names matching SPEC-FORMAT.md.</done>
</task>

</tasks>

<verification>
1. `grep -c "qa:monitor" README.md` returns 3+
2. `grep "qa:report --project" README.md` returns matches
3. `grep "qa:report.*--save" README.md` returns no matches
4. `grep "focus-management" README.md` returns 2+ matches (Tier 2 section + Accessibility Focus section)
5. `grep "page-structure" README.md` returns match in Tier 2 section
6. `grep "qa-monitor.md" README.md` returns match in Project Structure
7. `grep "hooks/" README.md` returns match in Project Structure
</verification>

<success_criteria>
- README documents all 6 skills: /qa:run, /qa:check, /qa:init, /qa:gen, /qa:monitor, /qa:report
- /qa:report examples show --project (required), --since, --create-issues, --format
- No stale flags (--save) appear in /qa:report examples
- Tier 2 accessibility bullets use canonical names: focus-management, page-structure, interactive-elements, form-accessibility
- Project Structure tree includes qa-monitor.md and hooks/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt-cleanup/07-01-SUMMARY.md`
</output>
