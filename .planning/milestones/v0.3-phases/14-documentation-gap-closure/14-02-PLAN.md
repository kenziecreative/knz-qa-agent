---
phase: 14-documentation-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/SPEC-FORMAT.md
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-05

must_haves:
  truths:
    - "A spec author reading SPEC-FORMAT.md can discover and use breakpoint-sweep: continuous"
    - "A spec author reading SPEC-FORMAT.md can discover and use placeholder_allowlist"
    - "A user reading README.md v2 Features sees Visual Focus, Design Reference, and Browsers listed"
    - "A contributor reading CLAUDE.md Agent Architecture sees v0.3 visual capabilities listed"
  artifacts:
    - path: "examples/SPEC-FORMAT.md"
      provides: "breakpoint-sweep: continuous and placeholder_allowlist field documentation"
      contains: "breakpoint-sweep: continuous"
    - path: "README.md"
      provides: "Visual Focus, Design Reference, Browsers in v2 Features"
      contains: "#### Visual Focus"
    - path: "CLAUDE.md"
      provides: "v0.3 Agent Architecture bullets"
      contains: "Visual & UX design verification"
  key_links:
    - from: "examples/SPEC-FORMAT.md placeholder_allowlist"
      to: "agents/qa-tester.md line 1783"
      via: "field behavior documentation matches agent implementation"
      pattern: "placeholder_allowlist"
    - from: "README.md #### Visual Focus"
      to: "examples/SPEC-FORMAT.md ### Visual Focus (v2)"
      via: "parallel feature documentation"
      pattern: "Visual Focus"
---

<objective>
Update SPEC-FORMAT.md with two missing field docs, add three v2 feature entries to README.md, and append v0.3 bullets to CLAUDE.md Agent Architecture.

Purpose: Closes four documentation gaps — MISSING-03 (breakpoint-sweep: continuous undocumented), MISSING-04 (placeholder_allowlist undocumented), MISSING-05 (README missing 3 v2 features), and MISSING-06 (CLAUDE.md missing v0.3 methodology).

Output: Updated examples/SPEC-FORMAT.md, README.md, and CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-documentation-gap-closure/14-RESEARCH.md
@.planning/phases/14-documentation-gap-closure/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add breakpoint-sweep: continuous and placeholder_allowlist to SPEC-FORMAT.md</name>
  <files>examples/SPEC-FORMAT.md</files>
  <read_first>
    - examples/SPEC-FORMAT.md (full file — 499 lines, need to see Visual Focus section at lines 226-255, Browsers section ending at line 308, and Test Scenarios at line 310)
    - agents/qa-tester.md (search for "breakpoint-sweep" near line 2024 and "placeholder_allowlist" near line 1783 to verify exact syntax)
    - examples/SPEC-FORMAT.md (lines 195-224 — Accessibility Focus section, for pattern reference)
  </read_first>
  <action>
Two insertions in examples/SPEC-FORMAT.md:

**Insertion A — breakpoint-sweep: continuous modifier**

After line 255 (end of the `### Visual Focus (v2)` specific-areas example block, the closing triple backtick), still inside the Visual Focus section, BEFORE line 257 (`### Design Reference (v2)`), insert:

```markdown

**Optional modifier -- continuous breakpoint sweep:**

When `performance-responsive` is listed, the default Phase 1 sweep tests at 6 discrete widths (320, 375, 768, 1024, 1280, 1440px). To also run a continuous sweep in ~50px increments (Phase 2), add `breakpoint-sweep: continuous` inside the `## Visual Focus` section:

```markdown
## Visual Focus
- performance-responsive
breakpoint-sweep: continuous
```

When `breakpoint-sweep: continuous` is present, continuous sweep activates from the start rather than only when a Phase 1 width fails.
```

**Insertion B — placeholder_allowlist field**

After line 308 (end of `### Browsers (v2)` section, the closing triple backtick of the example) and BEFORE line 310 (`### Test Scenarios`), insert a new section:

```markdown

### placeholder_allowlist (v2)

A top-level spec field that suppresses false-positive placeholder detections from the `layout-integrity` visual check. When the agent's placeholder regex matches text that is intentionally present (e.g., a legitimate "Learn more" CTA), listing it here prevents it from being reported as a finding.

**Syntax:**

```markdown
placeholder_allowlist: [TODO, Learn more]
```

**Behavior:**

- Field absent: All placeholder regex matches are reported normally
- Field present: Any placeholder finding whose matched text contains a listed string is suppressed
- Suppressed findings are noted in the report: "Suppressed N placeholder finding(s) matching spec allowlist"
- The field is only active when `layout-integrity` is included in `## Visual Focus`

**Example:**

```markdown
placeholder_allowlist: [TODO, Learn more, Click here]

## Visual Focus
- layout-integrity
```

Place `placeholder_allowlist` at the top level of the spec, outside any section header. Strings are matched as substrings -- if "Learn more" is in the list, any placeholder finding containing "Learn more" anywhere in its matched text is suppressed.
```

Note: The `(v2)` suffix in the heading follows the established convention for all v2 spec features in this document (see `### Accessibility Focus (v2)`, `### Visual Focus (v2)`, `### Design Reference (v2)`, `### Browsers (v2)`).
  </action>
  <verify>
    <automated>grep -c "breakpoint-sweep: continuous" examples/SPEC-FORMAT.md && grep -c "placeholder_allowlist" examples/SPEC-FORMAT.md && grep -c "### placeholder_allowlist (v2)" examples/SPEC-FORMAT.md</automated>
  </verify>
  <acceptance_criteria>
    - examples/SPEC-FORMAT.md contains the string "breakpoint-sweep: continuous" (at least 2 occurrences — heading text + code example)
    - examples/SPEC-FORMAT.md contains the heading "### placeholder_allowlist (v2)"
    - examples/SPEC-FORMAT.md contains the string "placeholder_allowlist: [TODO, Learn more]"
    - The breakpoint-sweep content appears within the Visual Focus section (before Design Reference)
    - The placeholder_allowlist section appears after Browsers (v2) and before Test Scenarios
    - grep "Suppressed N placeholder" examples/SPEC-FORMAT.md returns a match
  </acceptance_criteria>
  <done>SPEC-FORMAT.md documents both breakpoint-sweep: continuous and placeholder_allowlist with syntax, behavior, and examples</done>
</task>

<task type="auto">
  <name>Task 2: Add Visual Focus, Design Reference, and Browsers entries to README.md v2 Features</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 280-300 — the Accessibility Focus entry and the "See SPEC-FORMAT.md" closing line)
    - README.md (lines 196-215 — the v2 Features section start, for structural pattern reference)
    - examples/SPEC-FORMAT.md (lines 226-308 — Visual Focus, Design Reference, Browsers sections for verified content)
  </read_first>
  <action>
In README.md, insert three new `####` entries AFTER line 297 (the last line of the Accessibility Focus entry: `Can also be activated at generation time: `/qa:gen dashboard --a11y-depth deep``) and BEFORE line 299 (`See [examples/SPEC-FORMAT.md]...`).

Insert the following content verbatim:

```markdown

#### Visual Focus

Add a `## Visual Focus` section to activate Tier 2 structured visual verification for specific areas:

```markdown
## Visual Focus
- design-verification
- layout-integrity
```

Available areas: `design-verification`, `ux-states`, `layout-integrity`, `performance-responsive`

Without this section, Tier 1 baseline visual observations run during functional testing. With this section, the agent runs the full Tier 2 structured methodology for each listed area.

#### Design Reference

Add a `## Design Reference` section to provide mockup images for visual comparison during `design-verification`:

```markdown
## Design Reference

### desktop
- home-default: .qa/designs/desktop-home.png

### mobile
- home-default: .qa/designs/mobile-home.png
```

Each viewport gets a subsection. Each entry maps a state name to an image path (relative to project root). When absent, no design comparison runs (backward compatible).

#### Browsers

Add a `## Browsers` section to run the full test suite across multiple browser engines:

```markdown
## Browsers
- chromium
- webkit
- firefox
```

Valid engines: `chromium` (Chrome/Edge), `webkit` (Safari), `firefox` (Gecko). When absent, only Chromium runs. Activate browser selection at run time with `/qa:run --project <path> --browsers`.
```

The three entries follow the exact same pattern as the existing `#### Accessibility Focus` entry: `####` heading, prose description, fenced code block with spec syntax, bullet list or closing sentence.
  </action>
  <verify>
    <automated>grep -c "#### Visual Focus" README.md && grep -c "#### Design Reference" README.md && grep -c "#### Browsers" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the heading "#### Visual Focus" (exactly 1 occurrence)
    - README.md contains the heading "#### Design Reference" (exactly 1 occurrence)
    - README.md contains the heading "#### Browsers" (exactly 1 occurrence)
    - All three entries appear between "#### Accessibility Focus" and "See [examples/SPEC-FORMAT.md]"
    - The Visual Focus entry contains all four area names: design-verification, ux-states, layout-integrity, performance-responsive
    - The Browsers entry contains "chromium", "webkit", "firefox"
  </acceptance_criteria>
  <done>README.md v2 Features section lists all 8 v2 features including the three new visual ones</done>
</task>

<task type="auto">
  <name>Task 3: Append v0.3 bullets to CLAUDE.md Agent Architecture</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (lines 47-60 — the Agent Architecture section with its existing bullet list)
  </read_first>
  <action>
In CLAUDE.md, find the `### Agent Architecture` bullet list. The last bullet is currently on line 58:
```
- Spec v2 features (dependencies, data-driven scenarios, tags, environments)
```

Append two new bullets immediately after that line (before the blank line and "Skills orchestrate the agent" paragraph on line 60):

```markdown
- Visual & UX design verification (Tier 2 -- design reference comparison, UX states, layout integrity, performance/responsive, triggered by `## Visual Focus` in specs)
- Cross-browser engine testing (Chromium, WebKit, Firefox via `## Browsers` in specs)
```

These bullets follow the established format: `- [Capability name] ([parenthetical scope/detail])`. Do NOT modify any existing bullets. Only append.
  </action>
  <verify>
    <automated>grep -c "Visual & UX design verification" CLAUDE.md && grep -c "Cross-browser engine testing" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains the string "Visual & UX design verification (Tier 2"
    - CLAUDE.md contains the string "Cross-browser engine testing (Chromium, WebKit, Firefox"
    - Both new bullets appear after the "Spec v2 features" bullet and before "Skills orchestrate the agent"
    - All 9 original bullets remain unchanged (count total bullet lines in Agent Architecture = 11)
  </acceptance_criteria>
  <done>CLAUDE.md Agent Architecture section lists v0.3 visual capabilities alongside existing v0.2 capabilities</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan modifies documentation files only. No runtime code, no user input processing, no API endpoints.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | I (Information Disclosure) | README.md, CLAUDE.md, SPEC-FORMAT.md | accept | Documentation is public; no secrets or sensitive content added |
</threat_model>

<verification>
After all three tasks complete:
1. `grep "breakpoint-sweep: continuous" examples/SPEC-FORMAT.md` returns matches
2. `grep "placeholder_allowlist" examples/SPEC-FORMAT.md` returns matches
3. `grep "#### Visual Focus" README.md` returns exactly 1 match
4. `grep "#### Design Reference" README.md` returns exactly 1 match
5. `grep "#### Browsers" README.md` returns exactly 1 match
6. `grep "Visual & UX design verification" CLAUDE.md` returns 1 match
7. `grep "Cross-browser engine testing" CLAUDE.md` returns 1 match
</verification>

<success_criteria>
- SPEC-FORMAT.md documents breakpoint-sweep: continuous with syntax and behavior
- SPEC-FORMAT.md documents placeholder_allowlist with syntax, behavior, and example
- README.md v2 Features lists Visual Focus, Design Reference, and Browsers entries
- CLAUDE.md Agent Architecture has two new bullets for v0.3 capabilities
- All existing content in all three files is preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/14-documentation-gap-closure/14-02-SUMMARY.md`
</output>
