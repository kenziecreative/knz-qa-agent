---
phase: 14-documentation-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ORCHESTRATION.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "An orchestrating agent reading ORCHESTRATION.md can discover that visual testing capabilities exist"
    - "An orchestrating agent can find all four Tier 2 visual areas and understand when each activates"
    - "An orchestrating agent can find the --browsers flag in the /qa:run argument table"
    - "The Visual Testing section explains how Visual Focus parallels Accessibility Focus (Tier 1 vs Tier 2)"
  artifacts:
    - path: "docs/ORCHESTRATION.md"
      provides: "Visual Testing section and --browsers argument row"
      contains: "## Visual Testing"
  key_links:
    - from: "docs/ORCHESTRATION.md ## Visual Testing"
      to: "examples/SPEC-FORMAT.md ## Visual Focus"
      via: "shared terminology: design-verification, ux-states, layout-integrity, performance-responsive"
      pattern: "design-verification|ux-states|layout-integrity|performance-responsive"
    - from: "docs/ORCHESTRATION.md /qa:run arguments"
      to: "skills/run/SKILL.md Mode 8"
      via: "--browsers flag documentation"
      pattern: "--browsers"
---

<objective>
Add Visual Testing documentation to ORCHESTRATION.md so orchestrating agents and developers can discover and use v0.3 visual capabilities.

Purpose: ORCHESTRATION.md is the primary reference for AI orchestrators. Without a Visual Testing section, all Phase 9-12 visual methodology is invisible to orchestrating agents. This plan closes MISSING-01 (no Visual Testing section) and MISSING-02 (--browsers flag absent from argument table).

Output: Updated docs/ORCHESTRATION.md with a new `## Visual Testing` top-level section and a `--browsers` row in the `/qa:run` argument table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-documentation-gap-closure/14-RESEARCH.md
@.planning/phases/14-documentation-gap-closure/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --browsers row to /qa:run argument table</name>
  <files>docs/ORCHESTRATION.md</files>
  <read_first>
    - docs/ORCHESTRATION.md (lines 258-270 — the /qa:run Arguments table)
    - skills/run/SKILL.md (search for "Mode 8" and "--browsers" to verify exact flag syntax)
  </read_first>
  <action>
In docs/ORCHESTRATION.md, find the `/qa:run` argument table (currently at lines 260-268). Insert a new row for `--browsers` AFTER the `--env` row (line 266) and BEFORE the `task` row (line 267).

Exact row to insert:

```
| `--browsers` | No | from spec | Use browser engines from spec's `## Browsers` section (chromium, webkit, firefox). Defaults to chromium if `## Browsers` absent. |
```

The resulting table should have 7 data rows in this order: `--project`, `--spec`, `--url`, `--tag`, `--env`, `--browsers`, `task`. The footnote line "*Either `--project` or (`--url` + ad-hoc `task`) must be provided." stays unchanged at the bottom.

Also update the Quick Reference table at the top of the file (line ~15). The `/qa:run` row currently shows `--project`, `--spec`, `--url`, `--tag`, `--env` in the Key Arguments column. Add `--browsers` to that list so it reads: `--project`, `--spec`, `--url`, `--tag`, `--env`, `--browsers`.
  </action>
  <verify>
    <automated>grep -n "\-\-browsers" docs/ORCHESTRATION.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - docs/ORCHESTRATION.md contains the string "| `--browsers` | No | from spec |"
    - The --browsers row appears between the --env row and the task row in the argument table
    - The Quick Reference table /qa:run row contains "--browsers" in the Key Arguments column
  </acceptance_criteria>
  <done>The --browsers flag is documented in both the Quick Reference table and the /qa:run argument table in ORCHESTRATION.md</done>
</task>

<task type="auto">
  <name>Task 2: Add Visual Testing top-level section to ORCHESTRATION.md</name>
  <files>docs/ORCHESTRATION.md</files>
  <read_first>
    - docs/ORCHESTRATION.md (lines 478-484 — the gap between /qa:guide section end and Getting Started section)
    - docs/ORCHESTRATION.md (lines 27-119 — Interpreting Results section, for structural pattern reference)
    - agents/qa-tester.md (search for "Visual Focus" and "Tier 2" to verify area names and activation logic)
    - examples/SPEC-FORMAT.md (lines 226-308 — Visual Focus, Design Reference, Browsers sections for verified syntax)
  </read_first>
  <action>
In docs/ORCHESTRATION.md, insert a new `## Visual Testing` section AFTER line 482 (the `---` separator following the /qa:guide section) and BEFORE line 484 (`## Getting Started (For Developers)`). Include a `---` separator after the new section to match the document's existing pattern.

Insert the following content verbatim (this is the exact text from PATTERNS.md, verified against agents/qa-tester.md and examples/SPEC-FORMAT.md):

```markdown
## Visual Testing

Visual testing is Tier 2 opt-in functionality. Tier 1 visual observations (noting anomalies during functional testing) always run. Tier 2 structured visual checks activate only when a spec includes a `## Visual Focus` section — parallel to how `## Accessibility Focus` activates Tier 2 accessibility checks.

### Activating Visual Testing

Add a `## Visual Focus` section to the spec:

```markdown
## Visual Focus
- design-verification
- layout-integrity
```

Available areas:

| Area | What It Checks |
|------|---------------|
| `design-verification` | Compare live page against design reference images |
| `ux-states` | Verify empty, loading, error, and interaction states |
| `layout-integrity` | Spacing, alignment, grid integrity, content clipping |
| `performance-responsive` | Core Web Vitals, cross-browser rendering, viewport sweep |

- Present with no items listed: All four areas run (full Tier 2)
- Present with specific areas listed: Only those areas run
- Absent: Tier 1 only (visual anomalies noted during functional testing)

### Design Reference

Provide mockup images for visual comparison during `design-verification`. Add a `## Design Reference` section to the spec:

```markdown
## Design Reference

### desktop
- home-logged-out: .qa/designs/desktop-home.png

### mobile
- home-logged-out: .qa/designs/mobile-home.png
```

If a viewport subsection is absent, the agent skips visual diff for that viewport and notes "no design reference for [viewport]" in the report.

### Cross-Browser Testing

Specify which browser engines to run by adding a `## Browsers` section to the spec:

```markdown
## Browsers
- chromium
- webkit
- firefox
```

Valid engines: `chromium` (Chrome/Edge), `webkit` (Safari), `firefox` (Gecko). When the section is absent, only Chromium runs (backward compatible).

Invoke via: `/qa:run --project <path> --browsers`

The agent reports findings per engine: `[webkit] Scenario X: FAIL — rendering divergence on nav menu`

### Tier 2 Coverage Summary

| Area | Runs When |
|------|-----------|
| Design verification | `design-verification` in `## Visual Focus` AND `## Design Reference` section present |
| UX state verification | `ux-states` in `## Visual Focus` |
| Layout & content integrity | `layout-integrity` in `## Visual Focus` |
| Performance & responsive | `performance-responsive` in `## Visual Focus` |
| Breakpoint sweep (continuous) | `performance-responsive` active AND `breakpoint-sweep: continuous` in `## Visual Focus`, OR any Phase 1 breakpoint fails |

---
```

The section uses `##` level (not `###`) because it is a cross-cutting capability section, consistent with other top-level sections like `## Interpreting Results`, `## Workflow Integration`, `## Getting Started`.
  </action>
  <verify>
    <automated>grep -c "## Visual Testing" docs/ORCHESTRATION.md && grep -c "design-verification" docs/ORCHESTRATION.md && grep -c "breakpoint-sweep" docs/ORCHESTRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/ORCHESTRATION.md contains the heading "## Visual Testing" (exactly one occurrence)
    - The Visual Testing section appears AFTER the /qa:guide section and BEFORE "## Getting Started (For Developers)"
    - The section contains all four area names: "design-verification", "ux-states", "layout-integrity", "performance-responsive"
    - The section contains subsections: "### Activating Visual Testing", "### Design Reference", "### Cross-Browser Testing", "### Tier 2 Coverage Summary"
    - The Tier 2 Coverage Summary table has 5 rows (Design verification, UX state verification, Layout & content integrity, Performance & responsive, Breakpoint sweep)
    - grep "Visual Focus" docs/ORCHESTRATION.md returns at least 3 matches
    - grep "## Visual Testing" docs/ORCHESTRATION.md returns exactly 1 match
  </acceptance_criteria>
  <done>ORCHESTRATION.md has a complete Visual Testing section that orchestrating agents can discover and use to understand v0.3 visual capabilities</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan modifies documentation files only. No runtime code, no user input processing, no API endpoints.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | I (Information Disclosure) | docs/ORCHESTRATION.md | accept | Documentation is public; no secrets or sensitive content added |
</threat_model>

<verification>
After both tasks complete:
1. `grep "## Visual Testing" docs/ORCHESTRATION.md` returns exactly 1 match
2. `grep "\-\-browsers" docs/ORCHESTRATION.md` returns at least 2 matches (Quick Reference + argument table)
3. `grep "design-verification" docs/ORCHESTRATION.md` returns at least 1 match
4. `grep "ux-states" docs/ORCHESTRATION.md` returns at least 1 match
5. `grep "layout-integrity" docs/ORCHESTRATION.md` returns at least 1 match
6. `grep "performance-responsive" docs/ORCHESTRATION.md` returns at least 1 match
</verification>

<success_criteria>
- ORCHESTRATION.md has a Visual Testing section discoverable by orchestrating agents
- The /qa:run argument table includes the --browsers flag
- All four Tier 2 visual areas are documented with activation instructions
- Design Reference and Cross-Browser Testing subsections explain spec format usage
</success_criteria>

<output>
After completion, create `.planning/phases/14-documentation-gap-closure/14-01-SUMMARY.md`
</output>
