---
phase: 05-spec-format-v2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/qa-run.md
  - skills/qa-gen.md
autonomous: true

must_haves:
  truths:
    - "/qa:run --tag smoke filters scenarios to only those tagged smoke"
    - "/qa:run --env staging selects the staging environment profile"
    - "/qa:run passes dependency and data-driven context to the agent"
    - "/qa:gen --a11y-depth deep generates specs with Accessibility Focus section"
  artifacts:
    - path: "skills/qa-run.md"
      provides: "Skill with --tag and --env flag handling, dependency/data-driven execution flow"
      contains: "--tag"
    - path: "skills/qa-gen.md"
      provides: "Skill with --a11y-depth flag and v2 section generation"
      contains: "--a11y-depth"
  key_links:
    - from: "skills/qa-run.md"
      to: "agents/qa-tester.md"
      via: "qa-run passes filtered scenarios and active env profile to agent"
      pattern: "tag|env|environment"
    - from: "skills/qa-gen.md"
      to: "examples/SPEC-FORMAT.md"
      via: "Generated specs follow v2 format"
      pattern: "Accessibility Focus|tags|Environments"
---

<objective>
Update qa-run and qa-gen skills to support v2 spec features.

Purpose: Wire the user-facing commands to the v2 format — --tag for selective execution, --env for environment profiles, --a11y-depth for accessibility spec generation.
Output: Updated qa-run.md with --tag and --env flags plus dependency/data-driven execution instructions. Updated qa-gen.md with --a11y-depth flag and v2 section generation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-spec-format-v2/05-CONTEXT.md

@skills/qa-run.md
@skills/qa-gen.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --tag and --env flags to qa-run skill with v2 execution flow</name>
  <files>skills/qa-run.md</files>
  <action>
Update `skills/qa-run.md` to support v2 spec features. Keep ALL existing content intact — add to it.

**1. Update the frontmatter arguments line:**
Change from:
```
arguments: "[--project <path>] [--spec <name>] [--url <base-url>] [task]"
```
To:
```
arguments: "[--project <path>] [--spec <name>] [--url <base-url>] [--tag <tag>] [--env <profile>] [task]"
```

**2. Add new usage modes after Mode 4:**

```
### Mode 5: Run filtered by tag
/qa:run --project ~/Projects/my-app --tag smoke
Runs only scenarios tagged with `smoke` across all specs.

### Mode 6: Run with environment profile
/qa:run --project ~/Projects/my-app --spec checkout --env staging
Runs the checkout spec using the `staging` environment profile.

### Mode 7: Combined filtering
/qa:run --project ~/Projects/my-app --tag critical --env staging
Runs only `critical`-tagged scenarios against the staging environment.
```

**3. Update "Parsing Arguments" to add:**
- **tag**: Tag name to filter scenarios (optional). Only scenarios with this tag will run. Multiple tags can be comma-separated (e.g., `--tag smoke,critical`) — scenario must match ANY listed tag (OR logic).
- **env**: Environment profile name to activate (optional). Selects the matching profile from the spec's `## Environments` section.

**4. Update "Execution Flow > If a spec is provided" section:**

After step 2 (parse the spec), add new steps:

- 2a. **Environment resolution**: If `--env` flag provided, find the matching profile in `## Environments`. Extract base_url, credentials, timeouts. If profile not found, warn user and fall back to spec defaults. If no `--env` flag and spec has `## Environments`, use the first profile. If spec has no `## Environments`, use `## Base URL` (backward compatible).
- 2b. **Tag filtering**: If `--tag` flag provided, filter scenario list to only include scenarios whose `tags:` field contains at least one of the requested tags. If no scenarios match, inform user and exit. If no `--tag` flag, run all scenarios.
- 2c. **Dependency resolution**: After filtering, check `depends_on:` fields. If a filtered scenario depends on a scenario that was filtered OUT, include that dependency scenario automatically (it must run for the dependent to work). Inform the agent which scenarios were auto-included as dependencies.

**5. Update the Agent Invocation prompt template:**

Add to the structured prompt that gets passed to qa-tester agent:

```
{If environment profile active:}
## Active Environment
**Profile**: {profile_name}
**Base URL**: {profile.base_url}
**Credentials**: {profile.credentials or "use spec defaults"}
**Timeouts**: {profile.timeouts or "use defaults"}

{If tag filtering active:}
## Tag Filter
**Active tags**: {tag_list}
**Scenarios to run**: {filtered scenario names}
**Auto-included dependencies**: {dependency scenario names, if any}

{If spec has dependencies:}
## Execution Notes
- Scenarios have dependencies. If a dependency fails, skip dependent scenarios.
- Process scenarios in listed order — dependencies resolve top-to-bottom.

{If spec has data-driven scenarios:}
## Execution Notes
- Some scenarios have data sets. Run each data set as a separate variant.
- Report per-variant: "Scenario X [variant-name]: PASS/FAIL"
```

**6. Add new Examples at the end:**

```
**Run only smoke tests:**
/qa:run --project ~/Projects/store --tag smoke

**Run critical tests on staging:**
/qa:run --project ~/Projects/store --tag critical --env staging

**Run multiple tags:**
/qa:run --project ~/Projects/store --tag smoke,regression
```
  </action>
  <verify>
Read `skills/qa-run.md` and confirm:
- Arguments line includes --tag and --env
- Modes 5, 6, 7 documented
- Tag filtering logic described (OR logic for multiple tags)
- Environment profile resolution described with backward compatibility
- Dependency auto-inclusion after tag filtering described
- Agent prompt template includes environment, tag filter, and execution notes sections
- Existing modes 1-4 unchanged
  </verify>
  <done>
qa-run skill supports --tag and --env flags, handles dependency resolution after filtering, passes v2 context to agent in structured format, and maintains backward compatibility with v1 specs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add --a11y-depth flag and v2 section generation to qa-gen skill</name>
  <files>skills/qa-gen.md</files>
  <action>
Update `skills/qa-gen.md` to generate v2-format specs. Keep ALL existing content intact — add to it.

**1. Update the frontmatter arguments line:**
Change from:
```
arguments: "<spec-name> [--from <source>] [description]"
```
To:
```
arguments: "<spec-name> [--from <source>] [--a11y-depth <level>] [description]"
```

**2. Update "Parsing Arguments" to add:**
- **--a11y-depth**: Accessibility depth level (optional). Values: `baseline` (default, Tier 1 only), `deep` (includes `## Accessibility Focus` section in generated spec, triggering Tier 2 structured checks)

**3. Update the Generation Process sections:**

In EACH generation mode (From Description, From Documentation, From Live URL), add:

After the existing steps, add:
- If `--a11y-depth deep`: Add `## Accessibility Focus` section to generated spec listing all focus areas: `focus-management`, `page-structure`, `interactive-elements`, `form-accessibility`
- Add relevant `a11y` tag to scenarios that have accessibility implications

**4. Update the Output template** (the generated spec structure):

Add these new sections to the template, placed in the right order:

After `## Viewports` and before `## Test Scenarios`:

```markdown
## Environments (optional)
### local
- **base_url**: http://localhost:3000
- **credentials**: Use test accounts

### staging
- **base_url**: [TODO: Set staging URL]
- **credentials**: [TODO: Set staging credentials]
```

After `## Things to Watch For` and before the closing note:

```markdown
## Accessibility Focus (when --a11y-depth deep)
- focus-management
- page-structure
- interactive-elements
- form-accessibility
```

**5. Update the scenario template** to include tags:

Each generated scenario should include a `tags:` line. The generator should assign tags based on scenario type:
- First/happy path scenario: `tags: [smoke, critical]`
- Error/validation scenarios: `tags: [regression]`
- Edge case scenarios: `tags: [regression]`
- Accessibility-focused scenarios: `tags: [a11y]`

**6. Update the scenario template** to include dependencies where natural:

For scenarios that logically depend on prior scenarios (e.g., "Logout" depends on "Login"), add `depends_on:` field.

**7. Add new Examples at the end:**

```
**Generate spec with deep accessibility:**
/qa:gen dashboard --a11y-depth deep "Admin dashboard with data tables, filters, and export"

**Generate from app with accessibility focus:**
/qa:gen settings --from http://localhost:3000/settings --a11y-depth deep
```

**8. Update "After Generation" to mention:**
- New v2 sections (Environments, tags, Accessibility Focus) if generated
- That tags can be customized and additional tags added
- That environment profiles should be filled in with real values
  </action>
  <verify>
Read `skills/qa-gen.md` and confirm:
- Arguments line includes --a11y-depth
- Parsing section documents --a11y-depth with baseline/deep values
- Output template includes Environments section (as optional/template)
- Output template includes Accessibility Focus section (conditional on --a11y-depth deep)
- Scenario template includes tags: field
- Scenario template includes depends_on: where logical
- New examples showing --a11y-depth deep usage
- Existing generation modes unchanged
  </verify>
  <done>
qa-gen skill generates v2-format specs with tags on every scenario, dependency hints, environment profile templates, and full Accessibility Focus section when --a11y-depth deep is used.
  </done>
</task>

</tasks>

<verification>
- `grep -c "\-\-tag" skills/qa-run.md` returns > 0
- `grep -c "\-\-env" skills/qa-run.md` returns > 0
- `grep -c "a11y-depth" skills/qa-gen.md` returns > 0
- `grep -c "Accessibility Focus" skills/qa-gen.md` returns > 0
- `grep -c "tags:" skills/qa-gen.md` returns > 0
</verification>

<success_criteria>
1. qa-run supports --tag flag for scenario filtering with OR logic
2. qa-run supports --env flag for environment profile selection
3. qa-run handles dependency auto-inclusion after tag filtering
4. qa-run passes v2 context (env, tags, dependencies, data-driven) to agent
5. qa-gen supports --a11y-depth deep flag
6. qa-gen generates tags, dependencies, environment templates, and accessibility focus sections
7. Both skills maintain full backward compatibility with v1 usage
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-format-v2/05-02-SUMMARY.md`
</output>
