---
phase: 03-deep-web-testing
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - agents/qa-tester.md
  - skills/qa-run.md
  - skills/qa-gen.md
autonomous: true

must_haves:
  truths:
    - "Agent tests all validation states for forms (empty, invalid, boundary values)"
    - "Agent verifies error message placement and association with fields"
    - "Agent tests all submission states (loading, success, error, double-submit prevention)"
    - "Agent checks autofill behavior and field type attributes"
    - "Agent tests multi-step forms with back/forward navigation"
  artifacts:
    - path: "agents/qa-tester.md"
      provides: "Form Intelligence methodology section"
      contains: "Form Intelligence"
    - path: "skills/qa-run.md"
      provides: "Form testing instructions in agent prompt"
      contains: "form"
    - path: "skills/qa-gen.md"
      provides: "Form-specific edge cases in spec template"
      contains: "form"
  key_links:
    - from: "skills/qa-run.md"
      to: "agents/qa-tester.md"
      via: "Agent prompt references form testing methodology"
      pattern: "form|Form Intelligence"
---

<objective>
Add form intelligence methodology to the QA agent — the agent gains exhaustive form testing methodology covering validation boundaries, error messages, all four submission states, autofill, and multi-step form navigation.

Purpose: FORM-01 through FORM-05 — forms are where real bugs hide. The agent currently has basic edge case testing (empty inputs, boundary values) in its general methodology. This plan adds a dedicated, exhaustive form testing approach that treats every form as a first-class testing concern.

Output: Updated agent with Form Intelligence section, updated qa-run with form testing instructions, updated qa-gen with form-specific edge case templates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deep-web-testing/03-CONTEXT.md
@agents/qa-tester.md
@skills/qa-run.md
@skills/qa-gen.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Form Intelligence methodology to qa-tester agent</name>
  <files>agents/qa-tester.md</files>
  <action>
Add a new "## Form Intelligence" section to agents/qa-tester.md, placed after the Multi-Persona Testing section added in Plan 02.

The section should cover:

**When you encounter a form, test it exhaustively:**

**1. Validation Boundary Testing (FORM-01)**
For every input field, test:
- Empty submission — submit with the field blank (if required)
- Invalid formats — wrong email format, non-numeric in number fields, dates outside range
- Boundary values — min length, max length, one-over-max, special characters, Unicode, emoji
- Injection strings — SQL-like strings (`'; DROP TABLE --`), XSS attempts (`<script>alert(1)</script>`), HTML injection
- These aren't security tests (that's a separate concern) — they verify the UI handles bad input gracefully without crashing

**2. Error Message Quality (FORM-02)**
For every validation error triggered:
- Is the error message visible? (not hidden, not clipped, not behind another element)
- Is the error associated with the correct field? (appears near the field, not just at top of form)
- Does the field have `aria-invalid` or equivalent state?
- Is the error message helpful? ("Please enter a valid email" vs. "Error 422")
- Do errors clear when the user corrects the input?

**3. Submission States (FORM-03)**
Every form submission should test all four states:
- **Loading**: Is there a loading indicator? Is the submit button disabled during submission? Can the user submit twice?
- **Success**: Is there clear feedback? Does the UI update? Is the user redirected or shown a confirmation?
- **Error**: If the server returns an error, does the UI show a meaningful message? Does the form retain entered data?
- **Double-submit prevention**: Click submit twice rapidly — does the form handle it gracefully? (duplicate requests, duplicate entries)

**4. Autofill & Field Types (FORM-04)**
- Check that input fields have appropriate `type` attributes (`email`, `tel`, `password`, `number`, `date`)
- Check for `autocomplete` attributes on common fields (name, email, address, credit card)
- Verify that autofill doesn't break the form (some custom components conflict with browser autofill)
- Check that password fields mask input

**5. Multi-Step Forms (FORM-05)**
If the form has multiple steps/pages:
- Navigate forward through all steps
- Navigate backward — does previously entered data persist?
- Skip ahead if possible — what happens?
- Validate at each step — can you proceed with invalid data?
- Final submission — does it include data from all steps?

**Reporting form findings:**
- Validation failures that crash the form or show raw errors = High confidence
- Missing error messages or poor placement = Medium confidence
- Missing autofill attributes or field types = Low confidence
- Double-submit vulnerability = High confidence if it creates duplicate data
  </action>
  <verify>
grep -c "Form Intelligence" agents/qa-tester.md should return at least 1.
grep "Validation Boundary" agents/qa-tester.md should return a match.
grep "Submission States" agents/qa-tester.md should return a match.
grep "Double-submit" agents/qa-tester.md should return a match (case insensitive).
grep "Multi-Step" agents/qa-tester.md should return a match.
  </verify>
  <done>agents/qa-tester.md has a dedicated Form Intelligence section covering all 5 FORM requirements with exhaustive testing methodology.</done>
</task>

<task type="auto">
  <name>Task 2: Update qa-run and qa-gen for form testing</name>
  <files>skills/qa-run.md, skills/qa-gen.md</files>
  <action>
**Update skills/qa-run.md:**

In the agent prompt template (## Instructions section), add after the viewport/persona bullets:

```
- When testing forms, apply exhaustive form testing methodology:
  - Test all validation states (empty, invalid, boundary values, injection strings)
  - Verify error messages are visible, associated with correct fields, and helpful
  - Test all submission states (loading indicator, success feedback, error handling, double-submit prevention)
  - Check field type attributes and autofill behavior
  - For multi-step forms: test back/forward navigation and data persistence across steps
```

**Update skills/qa-gen.md:**

In the spec template (the "Structure the output as" section), update the "## Edge Cases to Verify" section to include form-specific edge cases. Add a subsection:

```markdown
### Form Testing (if forms are present)
- Empty submission of all required fields
- Invalid formats for typed fields (email, phone, date)
- Boundary values (min/max length, special characters)
- Rapid double-submit
- Multi-step navigation (if applicable): forward, backward, data persistence
```

This should be part of the generated template so specs automatically include form testing edge cases when the feature involves forms.
  </action>
  <verify>
grep "validation" skills/qa-run.md should return a match (case insensitive).
grep "double-submit" skills/qa-run.md should return a match (case insensitive).
grep "Form Testing" skills/qa-gen.md should return a match.
grep "Boundary" skills/qa-gen.md should return a match (case insensitive).
  </verify>
  <done>qa-run instructs the agent to apply exhaustive form testing methodology. qa-gen generates specs with form-specific edge case sections.</done>
</task>

</tasks>

<verification>
- agents/qa-tester.md contains Form Intelligence section with all 5 FORM requirements
- qa-run agent prompt includes form testing instructions
- qa-gen spec template includes form-specific edge cases
- Confidence framework applied to form findings (validation crashes = High, missing autofill = Low)
- All four submission states documented (loading, success, error, double-submit)
</verification>

<success_criteria>
FORM-01: Agent tests all validation states — covered by Validation Boundary Testing methodology
FORM-02: Agent verifies error message placement and association — covered by Error Message Quality methodology
FORM-03: Agent tests submission states — covered by all four states (loading, success, error, double-submit)
FORM-04: Agent checks autofill behavior and field types — covered by Autofill & Field Types methodology
FORM-05: Agent tests multi-step forms — covered by Multi-Step Forms methodology with back/forward/persistence
</success_criteria>

<output>
After completion, create `.planning/phases/03-deep-web-testing/03-03-SUMMARY.md`
</output>
