---
phase: 13-usage-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ORCHESTRATION.md
autonomous: true
requirements:
  - INFRA-04
must_haves:
  truths:
    - "An orchestrating agent can read ORCHESTRATION.md and know how to invoke every QA skill with correct arguments"
    - "An orchestrating agent can parse structured result blocks to determine pass/fail/concern outcomes"
    - "A developer new to the plugin can read ORCHESTRATION.md and understand how to use every skill without reading source files"
    - "ORCHESTRATION.md is self-contained — assumes reader has NOT seen README"
  artifacts:
    - path: "docs/ORCHESTRATION.md"
      provides: "Static reference doc for orchestrating agents and developers"
      min_lines: 200
      contains: "# QA Plugin Orchestration Guide"
  key_links:
    - from: "docs/ORCHESTRATION.md"
      to: "skills/*/SKILL.md"
      via: "documents invocation syntax and arguments for all 7 skills"
      pattern: "/qa:(run|check|init|gen|monitor|report|guide)"
---

<objective>
Create docs/ORCHESTRATION.md — a self-contained reference document teaching orchestrating AI agents and human developers how to use every QA plugin skill, interpret results, and integrate with workflows like GSD.

Purpose: INFRA-04 requires a static reference doc that makes the QA plugin usable without reading source code. This is the foundational artifact that /qa:guide (Plan 02) will also read from per D-07.

Output: docs/ORCHESTRATION.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-usage-guidance/13-CONTEXT.md
@.planning/phases/13-usage-guidance/13-PATTERNS.md

<interfaces>
<!-- Key patterns and conventions the executor needs from existing files -->

From skills/report/SKILL.md (argument table pattern):
```markdown
| Argument | Required | Default | Description |
|----------|----------|---------|-------------|
| `--project <path>` | Yes | — | Path to project containing `.qa/` directory |
```

From skills/check/SKILL.md (result output blocks):
```markdown
**If verification passes:**
✅ Phase {N} Verification: PASSED

**If verification fails:**
❌ Phase {N} Verification: FAILED

**If partial pass:**
⚠️ Phase {N} Verification: PASSED WITH CONCERNS
```

From README.md (skill listing pattern):
```markdown
### `/qa:SKILLNAME` - Short Title

One-sentence description.

```bash
# Example usage
/qa:SKILLNAME --project ~/Projects/my-app
```
```

From examples/SPEC-FORMAT.md (dual-audience doc structure):
```markdown
# QA Spec Format
Opening paragraph stating scope.
## Section Header
Tables for structured lookup, code blocks for machine-parseable content, prose for human context.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/ORCHESTRATION.md reference document</name>
  <files>docs/ORCHESTRATION.md</files>
  <read_first>
    - README.md (lines 28-123 for skill listing pattern and all skill descriptions)
    - examples/SPEC-FORMAT.md (dual-audience document structure pattern)
    - skills/report/SKILL.md (argument table pattern, cross-session capability pattern, files-read table pattern)
    - skills/check/SKILL.md (lines 90-150 for result output blocks — pass/fail/partial)
    - skills/run/SKILL.md (lines 1-50 for agent-spawning skill pattern and argument table)
    - skills/init/SKILL.md (full file — simple skill pattern)
    - skills/gen/SKILL.md (lines 1-30 for frontmatter and arguments)
    - skills/monitor/SKILL.md (lines 1-40 for frontmatter and arguments)
    - hooks/session-start.md (state inspection pattern — same checks /qa:guide will use)
    - hooks/stop.md (lines 1-30 for stop hook behavior summary)
    - .planning/phases/13-usage-guidance/13-CONTEXT.md (D-01 through D-03, D-08, D-09)
    - .planning/phases/13-usage-guidance/13-PATTERNS.md (full pattern assignments)
  </read_first>
  <action>
Create `docs/` directory and `docs/ORCHESTRATION.md` with the following structure. The document is self-contained per D-09 — assume the reader has NOT seen README.md. It is dual-audience per D-01/D-02: sections clearly labeled for AI agents (tables + code blocks) vs. human developers (narrative prose). It is fully additive per D-08 — no duplicated content with README, but skill descriptions are self-contained here (the reader should not need README).

**Document structure (in this order, per D-02 and the PATTERNS.md "urgency-first" principle):**

```
# QA Plugin Orchestration Guide

Opening paragraph: what this document covers, who it's for (orchestrating AI agents and human developers), and that the plugin provides 7 skills for QA testing web applications via Claude Code.

## Quick Reference (For Orchestrating Agents)

Table summarizing all 7 skills with columns: Skill, Purpose, Spawns Agent?, Key Arguments. This is the machine-parseable lookup table an agent reads first.

| Skill | Purpose | Spawns Agent? | Key Arguments |
|-------|---------|---------------|---------------|
| `/qa:init` | Scaffold .qa/ directory | No | `path` |
| `/qa:gen` | Generate spec from description/docs/URL | Yes | `name`, `--from`, `--a11y-depth` |
| `/qa:run` | Execute tests from specs or ad-hoc | Yes | `--project`, `--spec`, `--url`, `--tag`, `--env` |
| `/qa:check` | Verify phase deliverables (GSD gate) | Yes | `--url`, `--phase` |
| `/qa:monitor` | Recurring smoke tests via /loop | Yes | `--project`, `--url`, `--tag`, `--interval`, `--runs` |
| `/qa:report` | Cross-session report with trends | No | `--project`, `--format`, `--since`, `--create-issues` |
| `/qa:guide` | Context-aware usage guidance | No | `--project` |

## Interpreting Results

### Result Format

Show the three result output blocks an orchestrating agent will see:
- Pass block: `✅ Phase {N} Verification: PASSED` (from /qa:check) or clean run summary (from /qa:run)
- Fail block: `❌ Phase {N} Verification: FAILED` with Definite/Likely/Possible issue grouping
- Partial block: `⚠️ Phase {N} Verification: PASSED WITH CONCERNS`

### Confidence Levels

Table:
| Level | Meaning | Action |
|-------|---------|--------|
| High (Definite) | Confirmed real AND impactful | Fix before proceeding |
| Medium (Likely) | Probably real, needs investigation | Investigate, may not block |
| Low (Possible) | Worth noting, may be intentional | Note for review |

### Parsing Result Data

Code block showing how an agent should parse the structured output — look for the status line, extract issue counts, check for RECURRING flags in trend data.

## Workflow Integration

### Recommended Flow

Numbered steps for a typical project lifecycle:
1. `/qa:init` — scaffold .qa/ directory
2. `/qa:gen` — generate specs from feature descriptions
3. `/qa:run` — execute tests
4. `/qa:report` — review results and trends
5. `/qa:monitor` — set up recurring checks for deployed apps

### GSD Phase Integration

How to use /qa:check as a phase gate. Include the PROJECT.md snippet:
```markdown
## Verification
This is a web application. After completing each phase, run `/qa:check --url http://localhost:3000` to verify the deliverables work before proceeding.
```

Explain the 3-failure escalation rule and how an orchestrating agent should handle it.

### Decision Points for Orchestrating Agents

Flowchart-style guidance (as a decision list, not actual flowchart):
- "No .qa/ directory?" → run `/qa:init`
- "No spec files?" → run `/qa:gen`
- "Need to verify a phase?" → run `/qa:check --url <url> --phase N`
- "Need comprehensive testing?" → run `/qa:run --project <path>`
- "Want trend data?" → run `/qa:report --project <path>`
- "Deploying to staging/prod?" → run `/qa:monitor`
- "Unsure what to do?" → run `/qa:guide --project <path>`

## Skill Reference

Detailed per-skill sections. For each skill, include:
- One-sentence purpose
- Full argument table (copy structure from each SKILL.md's argument table)
- Usage examples in fenced bash blocks with # comment lines
- What it reads / what it produces
- When to use it vs. other skills

### /qa:init
### /qa:gen
### /qa:run
### /qa:check
### /qa:monitor
### /qa:report
### /qa:guide

## Getting Started (For Developers)

Narrative prose section for human readers:
- Installation: clone plugin, ensure playwright-cli installed
- First test: /qa:init then /qa:gen then /qa:run walkthrough
- Spec format: brief overview with pointer to examples/SPEC-FORMAT.md
- Reports: where they live (.qa/reports/), how to read them

## Data Architecture

Where QA data lives in a target project:
```
.qa/
├── *.md              # Spec files
├── reports/          # Test reports (generated)
│   └── HISTORY.md    # Running log
├── memory/           # QA-specific memory
├── .monitor-alert    # Regression alert marker (transient)
└── env.local         # Environment secrets (gitignored)
```

## Hooks (Automatic Behavior)

Brief explanation of session-start and stop hooks — what they do automatically so the reader understands the plugin's passive behavior.

## Cross-Session Capability

Note that /qa:report, /qa:guide, and session-start hooks all read from disk — no conversation context needed. Results accumulate across sessions.
```

IMPORTANT: Extract the actual argument tables from each SKILL.md file — do not invent arguments. Read each skill file and copy its exact arguments. For skills not yet read, read them during execution.

The document should be approximately 250-400 lines. Use tables for structured data, fenced code blocks for invocation examples and output formats, and narrative prose for the Getting Started section.
  </action>
  <verify>
    <automated>test -f docs/ORCHESTRATION.md && echo "EXISTS" && wc -l < docs/ORCHESTRATION.md && grep -c "^##" docs/ORCHESTRATION.md && grep -c "/qa:" docs/ORCHESTRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/ORCHESTRATION.md exists and has >= 200 lines
    - File contains `# QA Plugin Orchestration Guide` as the title
    - File contains `## Quick Reference` section with a table listing all 7 skills
    - File contains `## Interpreting Results` section with pass/fail/partial output blocks
    - File contains `## Workflow Integration` with GSD phase integration subsection
    - File contains `## Skill Reference` with subsections for each of the 7 skills: init, gen, run, check, monitor, report, guide
    - Each skill subsection contains an argument table with `| Argument | Required | Default | Description |` header
    - File contains `## Getting Started` section with narrative prose
    - File contains `## Data Architecture` section showing .qa/ directory structure
    - File contains `/qa:init`, `/qa:gen`, `/qa:run`, `/qa:check`, `/qa:monitor`, `/qa:report`, `/qa:guide` — all 7 skills mentioned
    - File does NOT contain `Agent` in allowed-tools for init, report, or guide skill descriptions (file-read-only skills)
    - grep -c "## " docs/ORCHESTRATION.md returns >= 10 (sufficient section depth)
    - grep "qa:guide" docs/ORCHESTRATION.md returns at least one match
  </acceptance_criteria>
  <done>docs/ORCHESTRATION.md exists as a self-contained reference document. An orchestrating agent can find invocation syntax, argument tables, and result-parsing instructions for all 7 QA skills. A developer can find a Getting Started walkthrough and skill descriptions without needing to read source files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation content | Static markdown — no executable code, no user input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | I (Information Disclosure) | docs/ORCHESTRATION.md | accept | Documentation is public/open-source. No secrets, credentials, or internal paths included. Example credentials use placeholder values only. |
| T-13-02 | T (Tampering) | docs/ORCHESTRATION.md | accept | File is version-controlled in git. Tampering is visible in diffs. No runtime execution risk — purely static reference. |
</threat_model>

<verification>
- docs/ORCHESTRATION.md exists with >= 200 lines
- All 7 skills documented with argument tables and usage examples
- Result format blocks present (pass/fail/partial)
- GSD integration section present
- Getting Started narrative section present for human audience
- Document is self-contained (no "see README for..." references)
</verification>

<success_criteria>
An orchestrating AI agent can read ORCHESTRATION.md and correctly invoke any QA skill with proper arguments, interpret the result output, and decide what to do next. A developer new to the plugin can read the Getting Started section and know how to use every skill.
</success_criteria>

<output>
After completion, create `.planning/phases/13-usage-guidance/13-01-SUMMARY.md`
</output>
