---
phase: 03-deep-web-testing
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - agents/qa-tester.md
  - skills/qa-run.md
  - skills/qa-gen.md
autonomous: true

must_haves:
  truths:
    - "Agent systematically runs each scenario at each viewport defined in spec"
    - "Agent detects responsive breakpoint issues (overlapping elements, hidden content)"
    - "Agent verifies touch target sizes at mobile viewports (minimum 44x44px)"
    - "Agent checks horizontal scrolling doesn't appear at any defined viewport"
    - "Agent runs scenarios as each persona defined in spec"
    - "Agent handles authentication flows per persona (login/logout between personas)"
    - "Agent verifies role-based content visibility (admin sees X, user sees Y)"
  artifacts:
    - path: "agents/qa-tester.md"
      provides: "Multi-Viewport and Multi-Persona methodology sections"
      contains: "Multi-Viewport"
    - path: "skills/qa-run.md"
      provides: "Viewport and persona orchestration instructions"
      contains: "viewport"
    - path: "skills/qa-gen.md"
      provides: "Viewport and persona sections in spec template"
      contains: "Viewports"
  key_links:
    - from: "skills/qa-run.md"
      to: "agents/qa-tester.md"
      via: "Agent prompt references viewport and persona methodology"
      pattern: "viewport|persona"
    - from: "skills/qa-gen.md"
      to: "skills/qa-run.md"
      via: "Generated specs include viewport/persona sections that qa-run parses"
      pattern: "Viewports|Personas"
---

<objective>
Add multi-viewport and multi-persona orchestration to the QA agent — the agent systematically tests every scenario at every viewport (default: mobile 375px, tablet 768px, desktop 1280px) and as every persona, with proper auth flow handling between persona switches.

Purpose: VIEW-01 through VIEW-04 and PERS-01 through PERS-03 — these are orchestration concerns: running the same scenarios across multiple dimensions (screen sizes and user roles). They naturally pair because both involve "run scenario X as/at Y" patterns.

Output: Updated agent with viewport and persona orchestration methodology, updated qa-run with orchestration instructions, updated qa-gen to generate specs with viewport and persona sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deep-web-testing/03-CONTEXT.md
@agents/qa-tester.md
@skills/qa-run.md
@skills/qa-gen.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Multi-Viewport and Multi-Persona methodology to qa-tester agent</name>
  <files>agents/qa-tester.md</files>
  <action>
Add two new sections to agents/qa-tester.md, placed after the Network Intelligence section added in Plan 01.

**## Multi-Viewport Testing**

Systematic approach to testing across screen sizes:

- **Default viewports:** mobile (375x812), tablet (768x1024), desktop (1280x800)
- Specs can override — narrow to specific viewports or add custom sizes (e.g., 1920x1080 for wide desktop)
- Every scenario runs at each viewport in the active set, unless the spec explicitly narrows it
- Use Playwright's `mcp__playwright__browser_resize` to change viewport between runs

**What to check at each viewport:**
- Responsive breakpoint issues — overlapping elements, text truncation, hidden content that shouldn't be hidden
- Touch target sizes at mobile viewports — interactive elements should be at least 44x44px. Use Playwright to get element bounding boxes and flag undersized targets
- Horizontal scrolling — should not appear at any defined viewport. Check document width vs viewport width
- Navigation patterns — hamburger menus at mobile, full nav at desktop
- Content reflow — text and images should reflow gracefully, not overlap or get clipped

**Reporting viewport findings:**
- Note which viewport the issue appears at
- Screenshot at the problematic viewport
- If an issue appears at only one viewport, it's likely a responsive CSS issue

**## Multi-Persona Testing**

Systematic approach to testing across user roles:

- Run scenarios as each persona defined in the spec
- Auth method is spec-defined per persona — some use full login flow (email/password), others may use token/cookie injection
- Between persona switches: log out current persona, clear relevant state, log in as next persona
- Use judgment on which scenarios benefit from multi-persona testing — not every scenario needs every persona (e.g., a public landing page test doesn't need admin persona)

**What to check per persona:**
- Role-based content visibility — admin sees admin controls, regular user does not. If spec defines expected content per role, verify it
- Permission-based actions — can admin delete? Can regular user? Test what each persona can and cannot do
- If an unauthorized persona can access restricted content, that's a High confidence finding (security issue)
- Auth flow itself — login works, session persists, logout clears session

**Orchestration order:**
- Run all scenarios at all viewports for persona A, then switch to persona B
- This minimizes auth flow overhead (fewer login/logout cycles)
- If a scenario fails for one persona, continue testing other personas — don't stop the run
  </action>
  <verify>
grep -c "Multi-Viewport" agents/qa-tester.md should return at least 1.
grep -c "Multi-Persona" agents/qa-tester.md should return at least 1.
grep "44x44" agents/qa-tester.md should return a match.
grep "375" agents/qa-tester.md should return a match (mobile viewport).
  </verify>
  <done>agents/qa-tester.md has dedicated Multi-Viewport Testing and Multi-Persona Testing sections covering all VIEW and PERS requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Update qa-run and qa-gen for viewport/persona orchestration</name>
  <files>skills/qa-run.md, skills/qa-gen.md</files>
  <action>
**Update skills/qa-run.md:**

1. In the "Execution Flow > If a spec is provided" section, the parsing step already mentions "Personas to test as" and "Viewports to test at" — good, these exist. Add clarity after step 3:

Add a new step between steps 3 and 4 (renumber as needed):
```
4. If spec defines viewports: Test each scenario at each viewport (default: mobile 375px, tablet 768px, desktop 1280px if spec doesn't specify)
5. If spec defines personas: Test scenarios as each persona, handling auth flows between switches
```

2. In the agent prompt template (## Instructions section), add after the network monitoring bullets:

```
- Test across viewports: Run each scenario at each viewport defined in the spec (default: mobile/tablet/desktop). Check for responsive issues, touch target sizes at mobile, and horizontal scrolling
- Test across personas: If spec defines personas, run scenarios as each persona. Handle login/logout between persona switches. Verify role-based content visibility
```

**Update skills/qa-gen.md:**

1. In the spec template structure (the "Structure the output as" section), the template already has a placeholder area. Ensure the template includes Viewports and Personas sections. Add between "## Base URL" and "## Test Scenarios":

```markdown
## Viewports
- Mobile: 375x812
- Tablet: 768x1024
- Desktop: 1280x800

## Personas
### Default User
- **Role:** Regular user
- **Auth:** [TODO: Set login credentials or auth method]

### Admin (if applicable)
- **Role:** Administrator
- **Auth:** [TODO: Set admin credentials or auth method]
```

2. Mark these sections with `[TODO: ...]` annotations so users know to customize them.
  </action>
  <verify>
grep "viewport" skills/qa-run.md should return matches.
grep "persona" skills/qa-run.md should return matches (case insensitive — "persona" or "Persona").
grep "Viewports" skills/qa-gen.md should return a match.
grep "Personas" skills/qa-gen.md should return a match.
  </verify>
  <done>qa-run instructs the agent to test across viewports and personas. qa-gen generates specs with Viewports and Personas sections by default.</done>
</task>

</tasks>

<verification>
- agents/qa-tester.md contains Multi-Viewport Testing and Multi-Persona Testing sections
- qa-run agent prompt includes viewport and persona orchestration instructions
- qa-gen spec template includes Viewports and Personas sections with sensible defaults
- Default viewports defined: mobile (375x812), tablet (768x1024), desktop (1280x800)
- Touch target minimum documented (44x44px)
- Auth flow handling documented (login/logout between persona switches)
</verification>

<success_criteria>
VIEW-01: Agent systematically tests each scenario at each viewport — covered by orchestration methodology and qa-run instructions
VIEW-02: Agent detects responsive breakpoint issues — covered by viewport check methodology
VIEW-03: Agent verifies touch target sizes at mobile — covered by 44x44px check
VIEW-04: Agent checks horizontal scrolling — covered by viewport check methodology
PERS-01: Agent runs scenarios as each persona — covered by persona orchestration
PERS-02: Agent handles auth flows per persona — covered by login/logout methodology
PERS-03: Agent verifies role-based content visibility — covered by persona check methodology
</success_criteria>

<output>
After completion, create `.planning/phases/03-deep-web-testing/03-02-SUMMARY.md`
</output>
